# 🔧 Финальное исправление деплоя на Vercel

## Проблема
Vercel не может найти `@vitejs/plugin-react` даже после добавления в `package.json`.

## ✅ Что уже исправлено

1. ✅ `@vitejs/plugin-react` добавлен в `web/package.json` (devDependencies)
2. ✅ `package-lock.json` обновлен и содержит пакет
3. ✅ `vercel.json` настроен с `rootDirectory: "web"`
4. ✅ Добавлены скрипты `preinstall` и `postinstall` для диагностики

## 🎯 Критическое решение

Проблема может быть в том, что **изменения не были закоммичены и запушены в репозиторий**. Vercel использует код из репозитория, а не локальные файлы.

### Шаг 1: Проверьте статус Git

```bash
git status
```

Убедитесь, что эти файлы закоммичены:
- `web/package.json`
- `web/package-lock.json`
- `vercel.json`

### Шаг 2: Закоммитьте и запушьте ВСЕ изменения

```bash
# Добавить все изменения
git add web/package.json web/package-lock.json vercel.json

# Проверить, что будет закоммичено
git status

# Закоммитить
git commit -m "Fix Vercel build: ensure @vitejs/plugin-react is installed"

# Запушить
git push
```

### Шаг 3: Очистите кеш Vercel

**В Vercel Dashboard:**
1. Откройте проект
2. **Settings** → **General**
3. Прокрутите до **Build & Development Settings**
4. Нажмите **Clear Build Cache** (если доступно)
5. Или в **Deployments** → выберите последний деплой → **Redeploy** → отключите **Use existing Build Cache**

### Шаг 4: Проверьте Root Directory в Dashboard

**В Vercel Dashboard:**
1. **Settings** → **General**
2. Найдите **Root Directory**
3. Должно быть: `web`
4. Если нет - установите и сохраните

## 🔍 Альтернативное решение (если проблема сохраняется)

Если после всех шагов проблема остается, попробуйте:

### Вариант A: Переместить `vercel.json` в `web/`

1. Переместите `vercel.json` в папку `web/`
2. Удалите `rootDirectory` из конфигурации (он больше не нужен)
3. В Vercel Dashboard установите **Root Directory** = `web`

### Вариант B: Использовать явную команду установки

В `vercel.json` измените:
```json
{
  "installCommand": "cd web && npm install && cd .."
}
```

Но это не должно быть нужно, если `rootDirectory` работает правильно.

## 📝 Проверка после исправления

В логах Vercel должно быть:
- ✅ `added X packages` (много пакетов, не 48)
- ✅ `@vitejs/plugin-react@4.x.x` в списке установленных
- ✅ `vite build` выполняется успешно
- ✅ `dist/` создается с файлами

## ⚠️ Важно

**Vercel использует код из репозитория, а не локальные файлы!** Если вы изменили файлы локально, но не закоммитили их, Vercel не увидит изменения.

---

**Статус**: Все файлы локально корректны. Нужно закоммитить и запушить изменения, затем очистить кеш Vercel.
