# 🌐 Cloudflare Tunnel - Быстрый старт

Альтернатива ngrok, которая работает из любой страны и полностью бесплатно!

## ✅ Преимущества

- ✅ Полностью бесплатно
- ✅ Работает из любой страны (нет блокировок IP)
- ✅ Постоянный URL (не меняется)
- ✅ Без регистрации для быстрого режима
- ✅ Без ограничений трафика

---

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Скачайте Cloudflare Tunnel

1. Откройте: https://github.com/cloudflare/cloudflared/releases/latest
2. Скачайте: `cloudflared-windows-amd64.exe`
3. Переименуйте в `cloudflared.exe`
4. Создайте папку `C:\cloudflared\`
5. Переместите туда `cloudflared.exe`

### Шаг 2: Запустите туннель

**Вариант A: Быстрый режим (без регистрации)**

Просто запустите в PowerShell:
```powershell
C:\cloudflared\cloudflared.exe tunnel --url http://localhost:8080
```

**Вариант B: Через скрипт**

```
SETUP_CLOUDFLARE_TUNNEL.bat
```

---

## 📋 Полная последовательность

### 1. Запустите backend (первое окно):
```
START_LOCAL_SERVER.bat
```

Дождитесь: "Backend starting on port 8080"

### 2. Запустите Cloudflare Tunnel (второе окно):

**PowerShell:**
```powershell
C:\cloudflared\cloudflared.exe tunnel --url http://localhost:8080
```

Или через скрипт:
```
SETUP_CLOUDFLARE_TUNNEL.bat
```

### 3. Скопируйте URL

Вы увидите что-то вроде:
```
+--------------------------------------------------------------------------------------------+
|  Your quick Tunnel has been created! Visit it at (it may take some time to be reachable):  |
|  https://abc123-random.cfargotunnel.com                                                   |
+--------------------------------------------------------------------------------------------+
```

**Скопируйте URL:** `https://abc123-random.cfargotunnel.com`

### 4. Обновите Vercel

1. Vercel Dashboard → Settings → Environment Variables
2. `VITE_API_URL` = ваш Cloudflare Tunnel URL
3. Redeploy

---

## 🔄 Автозапуск (опционально)

Создайте файл `START_CLOUDFLARE.bat`:

```batch
@echo off
start "Cloudflare Tunnel" C:\cloudflared\cloudflared.exe tunnel --url http://localhost:8080
```

Добавьте в Task Scheduler для автозапуска при включении ПК.

---

## 🆚 Сравнение с ngrok

| Функция | ngrok | Cloudflare Tunnel |
|---------|-------|-------------------|
| Бесплатно | ✅ | ✅ |
| Блокировки IP | ❌ (ваш случай) | ✅ Работает везде |
| Постоянный URL | ❌ (меняется) | ✅ Постоянный |
| Регистрация | Обязательна | Необязательна |
| Ограничения | Есть | Нет |

---

## ✅ Готово!

Cloudflare Tunnel работает без ограничений и полностью бесплатно!
