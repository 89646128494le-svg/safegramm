# 👤 Создание Owner аккаунта

Инструкция по созданию аккаунта владельца для SafeGram.

---

## 🚀 Быстрый способ (Windows)

### Просто запустите:

```
CREATE_OWNER.bat
```

Скрипт спросит:
- Username (по умолчанию: `owner`)
- Password (обязательно)
- Email (опционально)

---

## 📋 Ручной способ

### Шаг 1: Убедитесь что PostgreSQL запущен

```powershell
docker ps | findstr postgres
```

Если не запущен:
```powershell
docker start safegram-postgres
```

### Шаг 2: Перейдите в папку create-owner

```powershell
cd "C:\Users\Lev\Desktop\Проекты\SafeGram перезапуск\server-go\cmd\create-owner"
```

### Шаг 3: Создайте owner

```powershell
go run main.go owner ваш-пароль owner@example.com
```

**Пример:**
```powershell
go run main.go owner mypassword123 owner@safegram.com
```

**Или без email:**
```powershell
go run main.go owner mypassword123
```

---

## ✅ После создания

1. Зайдите на ваш сайт Vercel
2. Войдите с созданными данными:
   - Username: `owner` (или тот что указали)
   - Password: ваш пароль
3. После входа появится:
   - Вкладка **"Панель управления"** в меню
   - Вкладка **"Владелец"** в панели управления

---

## 🔧 Переменные окружения

Скрипт автоматически использует `DATABASE_URL` из `.env` файла в `server-go/`.

Если `.env` нет, используется значение по умолчанию:
```
postgres://safegram:safegram@localhost:5432/safegram?sslmode=disable
```

---

## 🆘 Проблемы

### Ошибка: "Failed to connect to database"

**Решение:**
1. Проверьте что PostgreSQL запущен:
   ```powershell
   docker ps | findstr postgres
   ```
2. Проверьте что база данных `safegram` существует
3. Проверьте `DATABASE_URL` в `.env`

### Ошибка: "User already exists"

**Решение:**
- Скрипт автоматически обновит существующего пользователя до роли `owner`
- Или используйте другое имя пользователя

---

## 📚 Подробная инструкция

См. `server-go/cmd/create-owner/README.md` для деталей.
