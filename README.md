# SafeGram — Безопасный мессенджер (Beta)

Современный мессенджер с end-to-end шифрованием, голосовыми звонками, группами, каналами и множеством функций.

## 🚀 Быстрый старт (Локально)

### Требования
- Docker Desktop
- Node.js 18+ (для разработки)

### Запуск

1. **Клонируйте репозиторий и перейдите в директорию**

2. **Настройте переменные окружения:**
   ```bash
   cp server/.env.example server/.env
   # Откройте server/.env и настройте JWT_SECRET и другие параметры
   ```

3. **Запустите приложение:**
   ```bash
   docker compose up -d
   ```

4. **Откройте в браузере:**
   - Веб-клиент: `http://localhost:8081`
   - API: `http://localhost:8080`
   - Health check: `http://localhost:8080/health`

## 🌐 Публичный релиз (Production)

### 🚀 Деплой на Vercel

**📋 Полная инструкция:** Откройте файл [`DEPLOY_AND_API_KEYS.md`](./DEPLOY_AND_API_KEYS.md)

**Быстрый старт:**

1. **Загрузите код в GitHub:**
   - Запустите `push-to-github.bat` (Windows)
   - Или следуйте инструкциям в `GIT_PUSH.md`

2. **Деплой на Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Импортируйте репозиторий `89646128494le-svg/safegram2`
   - Root Directory: `web`
   - Build Command: `npm run build`
   - Output Directory: `dist`

3. **Настройте переменные окружения:**
   - `VITE_API_URL` - URL вашего Backend API
   - `VITE_GIPHY_API_KEY` - API ключ Giphy (опционально)

**📚 Дополнительная документация:**
- [`DEPLOY_AND_API_KEYS.md`](./DEPLOY_AND_API_KEYS.md) - Полная инструкция по деплою и получению API ключей
- [`VERCEL_DEPLOYMENT.md`](./web/VERCEL_DEPLOYMENT.md) - Детальная инструкция по деплою
- [`INTEGRATION_GUIDE.md`](./INTEGRATION_GUIDE.md) - Интеграции для управления сервисами
- [`GITHUB_AUTHENTICATION.md`](./GITHUB_AUTHENTICATION.md) - Решение проблем с аутентификацией GitHub

**⚠️ ВАЖНО:** Перед публичным релизом обязательно:

1. **Установите JWT_SECRET:**
   ```bash
   # Сгенерируйте секретный ключ
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   # Добавьте в server/.env: JWT_SECRET=<сгенерированный-ключ>
   ```

2. **Настройте HTTPS** (рекомендуется)

3. **Настройте CORS** - укажите ваш домен в ALLOWED_ORIGINS

4. **Измените пароль админа** - установите ADMIN_PASS

## 📦 Структура проекта

```
SafeGram/
├── server/          # Backend (Node.js + Express + Socket.IO)
├── server-go/       # Backend (Go + Gin + PostgreSQL)
├── web/             # Frontend (React + TypeScript + Vite)
├── desktop/         # Desktop app (Electron)
├── docs/            # Документация
└── docker-compose.yml  # Docker конфигурация
```

## 🔧 Управление

### Остановка
```bash
docker compose down
```

### Пересборка после изменений
```bash
docker compose up -d --build
```

### Просмотр логов
```bash
docker compose logs -f server
docker compose logs -f web
```

### Проверка статуса
```bash
docker compose ps
curl http://localhost:8080/health
```

## ✨ Основные функции

- ✅ Личные чаты и группы
- ✅ End-to-end шифрование
- ✅ Голосовые и видеозвонки (WebRTC)
- ✅ Стикеры, GIF, эмодзи
- ✅ Геолокация
- ✅ Темы оформления
- ✅ Уведомления
- ✅ Каналы и подписки
- ✅ Голосовые комнаты
- ✅ Истории (Stories)
- ✅ Реакции и ответы
- ✅ Треды сообщений

## 🔒 Безопасность

- JWT аутентификация
- Rate limiting
- Content Security Policy (CSP)
- Валидация входных данных
- Защита от XSS и CSRF
- Шифрование паролей (scrypt)

## 📝 Замечания

- Для микрофона браузеру нужен HTTPS (кроме localhost)
- Для WebRTC через NAT может потребоваться TURN сервер
- Backend использует PostgreSQL для хранения данных
- Redis используется для кэширования и онлайн статусов

## 🔧 Разработка

### Backend (Go)
```bash
cd server-go
go run main.go
```

### Frontend (Web)
```bash
cd web
npm install
npm run dev
```

### Desktop App
```bash
cd desktop
npm install
npm run dev
```

## 📝 Git Setup

Для инициализации Git репозитория:

**Windows:**
```bash
init-git.bat
```

**Linux/Mac:**
```bash
chmod +x init-git.sh
./init-git.sh
```

После инициализации:
1. Создайте репозиторий на GitHub
2. Добавьте remote: `git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git`
3. Отправьте код: `git push -u origin main`

## 🐛 Известные проблемы (Beta)

- Некоторые функции могут работать нестабильно
- Производительность может снижаться при большом количестве пользователей
- Рекомендуется использовать для тестирования с ограниченным числом пользователей

## 📞 Поддержка

При проблемах проверьте:
1. Логи: `docker compose logs`
2. Health check: `curl http://localhost:8080/health`
3. Переменные окружения в `server/.env`

---

**Версия:** 1.0.0-beta | **Статус:** Готов к публичному тестированию
