# 🚨 Критическое исправление деплоя на Vercel

## Проблема
Vercel не может найти `@vitejs/plugin-react` даже после добавления в `package.json`.

## 🔍 Корневая причина

**Вероятная причина**: Vercel может устанавливать зависимости с `NODE_ENV=production`, что пропускает `devDependencies`. Но `@vitejs/plugin-react` нужен для сборки, даже если он в `devDependencies`.

## ✅ Решение (применено)

1. ✅ Создан `web/vercel.json` - конфигурация в правильной папке
2. ✅ Изменен `installCommand` на `npm install --include=dev` - явно устанавливает devDependencies
3. ✅ `@vitejs/plugin-react` есть в `package.json` (devDependencies)

## 🎯 Что нужно сделать СЕЙЧАС

### Шаг 1: Закоммитьте и запушьте ВСЕ изменения

```bash
# Проверьте статус
git status

# Добавьте все изменения
git add web/package.json web/package-lock.json web/vercel.json vercel.json

# Закоммитьте
git commit -m "Fix Vercel build: add @vitejs/plugin-react and configure install command"

# Запушьте
git push
```

### Шаг 2: Настройте Root Directory в Vercel Dashboard

**КРИТИЧЕСКИ ВАЖНО:**

1. Откройте **Vercel Dashboard**
2. Выберите ваш проект
3. **Settings** → **General**
4. Найдите **Root Directory**
5. Установите: `web`
6. **Сохраните изменения**

### Шаг 3: Очистите кеш Vercel

**В Vercel Dashboard:**
1. **Deployments** → выберите последний деплой
2. Нажмите **"..."** (три точки) → **Redeploy**
3. **ОТКЛЮЧИТЕ** "Use existing Build Cache"
4. Нажмите **Redeploy**

Или:
- **Settings** → **General** → **Clear Build Cache** (если доступно)

### Шаг 4: Проверьте логи

После пересборки в логах должно быть:
- ✅ `added X packages` (много пакетов, не 48)
- ✅ `@vitejs/plugin-react@4.x.x` в списке установленных
- ✅ `vite build` выполняется успешно
- ✅ `dist/` создается с файлами

## 🔧 Альтернативное решение (если не помогло)

Если проблема сохраняется, попробуйте переместить `@vitejs/plugin-react` в `dependencies` вместо `devDependencies`:

```json
{
  "dependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    ...
  }
}
```

Но это не рекомендуется, так как пакет нужен только для сборки.

## ⚠️ КРИТИЧЕСКИ ВАЖНО

1. **Root Directory ДОЛЖЕН быть установлен в Dashboard** = `web`
2. **Все изменения ДОЛЖНЫ быть закоммичены и запушены**
3. **Кеш ДОЛЖЕН быть очищен**

Без этих шагов исправления не сработают!

---

**Статус**: ✅ Конфигурация исправлена. Нужно закоммитить, настроить Root Directory в Dashboard и очистить кеш.
