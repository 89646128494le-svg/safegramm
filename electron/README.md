# 🖥️ SafeGram Desktop

Нативное desktop приложение для SafeGram, созданное с помощью Electron.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
# Убедитесь, что веб-приложение запущено
cd ../web
npm run dev

# В другом терминале
cd ../electron
npm start
```

### Сборка для production
```bash
# Сначала соберите веб-приложение
cd ../web
npm run build

# Затем соберите Electron
cd ../electron
npm run build
```

## 📦 Сборка для разных платформ

- **Windows**: `npm run build:win`
- **macOS**: `npm run build:mac`
- **Linux**: `npm run build:linux`

## ⚙️ Конфигурация

### Изменение URL приложения

В `main.js` измените `startUrl`:

```javascript
const startUrl = isDev 
  ? 'http://localhost:5173' 
  : 'https://your-vercel-app.vercel.app'; // Ваш production URL
```

### Иконки

Поместите иконки в директорию `electron/`:
- `icon.ico` - Windows (256x256)
- `icon.icns` - macOS (512x512)
- `icon.png` - Linux (512x512)

## 🔄 Автообновления

Автообновления настроены через `electron-updater`. Для работы нужен сервер обновлений (GitHub Releases или собственный).

## 📚 Документация

Полная документация: [DESKTOP_APP_GUIDE.md](../DESKTOP_APP_GUIDE.md)

---

**Версия**: 1.0.0
