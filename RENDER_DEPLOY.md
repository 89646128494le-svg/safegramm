# 🎨 Деплой SafeGram Backend на Render

Пошаговая инструкция деплоя Go backend на Render.com

## 🚀 Быстрый деплой (5 минут)

### Шаг 1: Регистрация

1. Зайдите на https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub
4. Авторизуйте Render доступ к GitHub

### Шаг 2: Создание Web Service

1. Нажмите **"New +"** в верхнем правом углу
2. Выберите **"Web Service"**
3. Нажмите **"Connect account"** если нужно
4. Найдите репозиторий `89646128494le-svg/safegramm`
5. Нажмите **"Connect"**

### Шаг 3: Настройка сервиса

Заполните форму:

- **Name:** `safegram-backend` (или любой)
- **Region:** `Frankfurt` (ближайший к России, или выбирайте по вашему расположению)
- **Branch:** `main`
- **Root Directory:** `server-go` ⚠️ **ВАЖНО!**
- **Runtime:** `Go`
- **Build Command:** `go build -o main .`
- **Start Command:** `./main`
- **Plan:** `Free` ✅

### Шаг 4: Переменные окружения

Пока не нажимайте "Create Web Service"!

Прокрутите вниз до "Environment Variables" и добавьте:

```
JWT_SECRET=сгенерируйте-случайную-строку-32-символа
PORT=8080
WEBHOOK_URL=http://localhost:3000/webhook
NODE_ENV=production
```

**Генерация JWT_SECRET (PowerShell):**
```powershell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

**DATABASE_URL добавим после создания базы!**

### Шаг 5: Создание PostgreSQL базы

1. Еще НЕ создавайте Web Service
2. В новом окне/вкладке откройте Render
3. Нажмите **"New +"** → **"PostgreSQL"**
4. Заполните:
   - **Name:** `safegram-db`**
   - **Database:** `safegram` (или любое)
   - **User:** `safegram` (или любое)
   - **Region:** `Frankfurt` (тот же что и сервис)
   - **Plan:** `Free` ✅
5. Нажмите **"Create Database"**
6. Дождитесь создания базы (1-2 минуты)

### Шаг 6: Подключение базы к сервису

1. Откройте созданную PostgreSQL базу
2. На вкладке **"Connections"** найдите **"Internal Database URL"**
3. Скопируйте URL (например: `postgresql://safegram:password@dpg-xxx.a.singapore-postgres.render.com/safegram`)
4. Вернитесь к настройке Web Service
5. В **Environment Variables** добавьте:
   ```
   DATABASE_URL=postgresql://safegram:password@dpg-xxx.a.singapore-postgres.render.com/safegram
   ```
   (вставьте скопированный Internal Database URL)

### Шаг 7: Создание Web Service

1. Нажмите **"Create Web Service"**
2. Дождитесь деплоя (3-5 минут)
3. Render автоматически создаст URL вида: `https://safegram-backend.onrender.com`

### Шаг 8: Проверка

Откройте в браузере:
```
https://safegram-backend.onrender.com/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":{}}
```

### Шаг 9: Обновление Frontend

1. Зайдите в Vercel Dashboard
2. Settings → Environment Variables
3. Добавьте/обновите:
   ```
   VITE_API_URL=https://safegram-backend.onrender.com
   ```
4. Перезапустите деплой

## ✅ Готово!

Backend задеплоен на Render и готов к работе!

---

## ⚠️ Важные моменты

### Sleep режим

Render бесплатный план засыпает после **15 минут** бездействия.

**Решение:**
- Первый запрос после сна займет ~30 секунд
- Последующие запросы будут быстрыми
- Можно использовать сервисы типа UptimeRobot для "пробуждения" (опционально)

### Автоматический деплой

Render автоматически деплоит при push в `main` branch.

### Логи

Просмотр логов:
1. Откройте сервис в Render Dashboard
2. Вкладка **"Logs"**
3. Видите логи в реальном времени

### SSL сертификаты

Render автоматически выдает SSL сертификаты для вашего домена.

---

## 🔧 Дополнительные настройки

### CORS

Добавьте в Environment Variables:
```
ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://safegram-backend.onrender.com
```

### Health Check

Render автоматически проверяет здоровье приложения. Убедитесь что endpoint `/health` работает.

---

## 💰 Бесплатный план

Render бесплатный план включает:
- ✅ 750 часов работы/месяц (хватает на 24/7)
- ✅ PostgreSQL база данных
- ✅ SSL сертификаты
- ✅ Автоматический деплой
- ⚠️ Sleep после 15 минут бездействия

---

## 📚 Troubleshooting

### Проблема: Долгий первый запрос

**Причина:** Приложение заснуло  
**Решение:** Подождите 30 секунд, следующие запросы будут быстрыми

### Проблема: Build failed

**Решение:**
- Проверьте Root Directory: должен быть `server-go`
- Проверьте Build Command: `go build -o main .`
- Проверьте логи в Render Dashboard

### Проблема: Database connection failed

**Решение:**
- Убедитесь что используете **Internal Database URL** (не External)
- Проверьте что база и сервис в одном регионе
- Проверьте переменную окружения `DATABASE_URL`

---

## ✅ После деплоя

1. ✅ Backend работает на Render
2. ✅ Frontend обновлен с новым API URL
3. ✅ Мессенджер полностью работает! 🎉
