# SafeGram Desktop Application

Профессиональное десктопное приложение для SafeGram с нативной производительностью и полной интеграцией всех функций платформы.

## 🚀 Особенности

- **Производительность**: Оптимизировано для работы с большими объемами данных
- **Надежность**: Автоматическое переподключение, обработка ошибок, очередь сообщений
- **Безопасность**: Шифрование данных, безопасное хранение токенов
- **UX**: Нативные уведомления, системный трей, горячие клавиши
- **Кроссплатформенность**: Windows, macOS, Linux

## 📋 Требования

- Node.js 18+
- npm или yarn
- Go сервер SafeGram (запущен на http://localhost:8080)

## 🛠️ Установка

### Быстрая установка

```bash
# 1. Установка зависимостей
cd desktop
npm install
cd renderer
npm install
cd ..

# 2. Запуск в режиме разработки
npm run dev
```

### Детальная установка

См. [INSTALL.md](./INSTALL.md) для подробных инструкций.

## 🎯 Быстрый старт

См. [QUICKSTART.md](./QUICKSTART.md) для быстрого старта.

## 📁 Структура проекта

```
desktop/
├── main/                    # Electron main process
│   ├── main.js              # Точка входа
│   ├── window-manager.js    # Управление окнами
│   └── tray-manager.js      # Системный трей
├── renderer/                # React приложение
│   ├── src/
│   │   ├── core/            # Ядро приложения
│   │   │   ├── api/         # HTTP клиент
│   │   │   ├── websocket/   # WebSocket менеджер
│   │   │   └── cache/       # Кэширование (IndexedDB)
│   │   ├── components/      # UI компоненты
│   │   ├── pages/           # Страницы
│   │   └── types/           # TypeScript типы
│   └── dist/                # Собранное приложение
└── dist/                    # Собранное Electron приложение
```

## 🏗️ Архитектура

См. [ARCHITECTURE.md](./ARCHITECTURE.md) для подробного описания архитектуры.

## 🔧 Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

Это запустит:
- Electron main process
- Vite dev server для renderer process (http://localhost:5173)

### Сборка

```bash
# Сборка renderer
cd renderer
npm run build
cd ..

# Сборка Electron приложения
npm run build
```

Собранное приложение будет в `desktop/dist/`.

### Добавление новых функций

1. **API методы**: Добавьте в `renderer/src/core/api/client.ts`
2. **WebSocket события**: Обработайте в `renderer/src/core/websocket/manager.ts`
3. **UI компоненты**: Создайте в `renderer/src/components/`
4. **Страницы**: Создайте в `renderer/src/pages/`

## 📚 Документация

- [INSTALL.md](./INSTALL.md) - Установка и настройка
- [QUICKSTART.md](./QUICKSTART.md) - Быстрый старт
- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура приложения
- [NEXT_STEPS.md](./NEXT_STEPS.md) - Следующие шаги разработки

## 🎨 Основные компоненты

### API Client (`core/api/client.ts`)

Профессиональный HTTP клиент с:
- Автоматическими повторными попытками
- Обработкой ошибок
- Таймаутами
- Загрузкой файлов с прогрессом

### WebSocket Manager (`core/websocket/manager.ts`)

Надежный WebSocket клиент с:
- Автоматическим переподключением
- Очередью сообщений
- Ping/Pong для проверки соединения
- Подпиской на события

### Cache Database (`core/cache/database.ts`)

Локальная база данных (IndexedDB) для:
- Кэширования данных
- Офлайн режима
- Синхронизации

## 🔒 Безопасность

- Context isolation включен
- Node integration отключен
- Preload скрипт для безопасного API
- Валидация всех входных данных
- Шифрование локальных данных (планируется)

## ⚡ Производительность

- Виртуализация списков (планируется)
- Ленивая загрузка компонентов
- Кэширование данных
- Оптимизация памяти

## 🐛 Отладка

### Main Process
Используйте `console.log` в `main/main.js`. Логи будут видны в терминале.

### Renderer Process
Откройте DevTools:
- **macOS**: `Cmd + Option + I`
- **Windows/Linux**: `Ctrl + Shift + I`

## 📝 Следующие шаги

См. [NEXT_STEPS.md](./NEXT_STEPS.md) для списка задач и приоритетов.

## 🤝 Вклад

При разработке следуйте архитектурным принципам, описанным в [ARCHITECTURE.md](./ARCHITECTURE.md).

## 📄 Лицензия

См. основной README проекта.
