{
  "name": "safegram",
  "private": true,
  "description": "SafeGram — защищённый мессенджер. Бэкенд: Go (бинарный протокол, E2EE). Фронт: Next.js, Qt.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "dev:web": "npm run dev --prefix web",
    "dev:web:standalone": "cd web && npm run dev",
    "dev:server": "go run ./cmd/server",
    "dev:server-go": "cd server-go && go run .",
    "dev:desktop": "cd desktop && npm run dev",
    "dev:desktop:legacy": "cd electron && npm run start:standalone",
    "dev:mobile": "cd rn && npm run start",
    "dev:all": "concurrently -n server,web -c blue,green \"npm run dev:server\" \"npm run dev:web\"",
    "dev:all:core": "concurrently -n server,web -c blue,green \"npm run dev:server-go\" \"npm run dev:web:standalone\"",
    "build": "npm run build --prefix web",
    "build:server": "go build -o bin/server ./cmd/server",
    "preview": "npm run preview --prefix web",
    "postinstall": "npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
