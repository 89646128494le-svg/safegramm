# 🔧 Исправление деплоя на Vercel

## Проблема

Vercel пытается собрать проект из корня, но проект находится в папке `web/`. В логах видно, что установлено только 48 пакетов вместо сотен, что означает неправильную директорию.

## ✅ Решение (применено)

Обновлен корневой `vercel.json` с добавлением `rootDirectory: "web"`. Теперь Vercel будет:
1. Переходить в папку `web/`
2. Устанавливать зависимости из `web/package.json`
3. Собирать проект из `web/`
4. Использовать `web/dist` как output directory

## 🚀 Альтернативное решение (если не работает)

Если `rootDirectory` не работает, настройте проект в Vercel Dashboard:

1. Откройте **Project Settings** → **General**
2. В разделе **Root Directory** выберите `web`
3. Сохраните изменения
4. Пересоберите проект

## 📝 Проверка

После исправления в логах должно быть:
- ✅ Установлено много пакетов (сотни, не 48)
- ✅ Команда `vite build` выполняется успешно
- ✅ Файлы собираются в `dist/`

## 🔄 Пересборка

После исправления `vercel.json`:
1. Закоммитьте изменения
2. Запушьте в репозиторий
3. Vercel автоматически пересоберет проект

Или вручную:
```bash
cd web
vercel --prod
```

---

**Статус**: ✅ Исправлено - добавлен `rootDirectory: "web"`
