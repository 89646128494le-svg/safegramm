# 🔧 Исправление ошибки сборки на Vercel

## ❌ Проблема

```
sh: line 1: vite: command not found
```

Vercel пытается запустить Vite, но он не найден, потому что проект находится в папке `web/`.

---

## ✅ Решение

### Вариант 1: Настроить Root Directory в Vercel (РЕКОМЕНДУЕТСЯ)

1. Зайдите в **Vercel Dashboard**
2. Ваш проект → **Settings**
3. **General** → **Root Directory**
4. Выберите `web` или введите `web`
5. Нажмите **Save**
6. Перезапустите деплой:
   - **Deployments** → последний → **"..."** → **Redeploy**

После этого Vercel будет собирать проект из папки `web/` и найдет Vite.

### Вариант 2: Обновить vercel.json

Упростите `vercel.json` - если Root Directory установлен в `web`, команды должны быть без `cd web`:

```json
{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

Или если Root Directory не установлен, оставьте как есть, но добавьте проверку:

```json
{
  "version": 2,
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/dist",
  "installCommand": "cd web && npm install",
  "framework": "vite"
}
```

---

## 📋 Пошаговая инструкция

### Шаг 1: Проверьте Root Directory

1. Vercel Dashboard → Проект → **Settings** → **General**
2. Проверьте **Root Directory**
3. Должно быть: `web`
4. Если нет - установите `web` и сохраните

### Шаг 2: Обновите vercel.json (если нужно)

Если Root Directory = `web`, упростите `vercel.json`.

### Шаг 3: Перезапустите деплой

1. **Deployments** → последний деплой
2. **"..."** → **Redeploy**

---

## ✅ Проверка

После исправления:
- ✅ Сборка должна пройти успешно
- ✅ Vite должен быть найден
- ✅ Проект должен собраться

---

## 🆘 Если все равно не работает

### Проблема: Root Directory не сохраняется

**Решение:**
- Убедитесь что папка `web` существует в репозитории
- Проверьте что в `web/` есть `package.json` с Vite

### Проблема: Все равно "vite: command not found"

**Решение:**
1. Убедитесь что `web/package.json` содержит Vite в `devDependencies`:
   ```json
   "devDependencies": {
     "vite": "^5.4.8"
   }
   ```
2. Убедитесь что Root Directory установлен в `web`
3. Попробуйте очистить кэш и перезапустить деплой

---

## 📚 Важно

**Root Directory** должен быть `web` - это самая важная настройка!

После установки Root Directory, все команды будут выполняться из папки `web/`, и Vite будет найден автоматически.
