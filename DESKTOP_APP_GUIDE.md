# 🖥️ Руководство по созданию Desktop версии SafeGram

## Обзор

SafeGram Desktop - это нативное desktop приложение, созданное с помощью Electron, которое оборачивает веб-приложение в desktop оболочку.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd electron
npm install
```

### 2. Запуск в режиме разработки

```bash
# Убедитесь, что веб-приложение запущено на localhost:5173
cd ../web
npm run dev

# В другом терминале запустите Electron
cd ../electron
npm start
```

### 3. Сборка для production

```bash
# Сначала соберите веб-приложение
cd ../web
npm run build

# Затем соберите Electron приложение
cd ../electron
npm run build
```

## 📦 Сборка для разных платформ

### Windows
```bash
npm run build:win
```

Создаст:
- `dist/SafeGram Setup x.x.x.exe` - установщик NSIS
- `dist/SafeGram x.x.x.exe` - portable версия

### macOS
```bash
npm run build:mac
```

Создаст:
- `dist/SafeGram-x.x.x.dmg` - установщик
- `dist/SafeGram-x.x.x-mac.zip` - архив

### Linux
```bash
npm run build:linux
```

Создаст:
- `dist/SafeGram-x.x.x.AppImage` - AppImage
- `dist/safegram-x.x.x.deb` - Debian пакет
- `dist/safegram-x.x.x.rpm` - RPM пакет

## 🔧 Конфигурация

### Изменение настроек приложения

Отредактируйте `electron/package.json`:

```json
{
  "build": {
    "appId": "com.safegram.desktop",
    "productName": "SafeGram",
    "win": {
      "icon": "icon.ico"
    },
    "mac": {
      "icon": "icon.icns"
    }
  }
}
```

### Изменение URL приложения

В `electron/main.js` измените `startUrl`:

```javascript
const startUrl = isDev 
  ? 'http://localhost:5173' 
  : 'https://your-vercel-app.vercel.app'; // Или ваш production URL
```

## 🎨 Иконки

### Требуемые размеры

- **Windows**: `icon.ico` (256x256, многоразмерный)
- **macOS**: `icon.icns` (512x512, многоразмерный)
- **Linux**: `icon.png` (512x512)

### Генерация иконок

Используйте инструменты:
- [Electron Icon Maker](https://www.electron.build/icons)
- [Icon Generator](https://icon.kitchen/)

## 🔄 Автообновления

Автообновления настроены через `electron-updater`. Для работы нужен:

1. **Сервер обновлений** (GitHub Releases, собственный сервер)
2. **Настройка в package.json**:

```json
{
  "build": {
    "publish": {
      "provider": "github",
      "owner": "your-username",
      "repo": "safegram"
    }
  }
}
```

## 📱 Интеграция с системой

### Уведомления

Desktop версия использует нативные системные уведомления через Web Notifications API.

### Горячие клавиши

Горячие клавиши работают через веб-приложение. Для системных горячих клавиш используйте Electron Menu API.

### Автозапуск

Для автозапуска при старте системы используйте:

```javascript
const { app } = require('electron');
app.setLoginItemSettings({
  openAtLogin: true
});
```

## 🐛 Отладка

### DevTools

В режиме разработки DevTools открываются автоматически. В production:

```javascript
// В main.js
mainWindow.webContents.openDevTools();
```

### Логи

Логи Electron доступны в консоли терминала при запуске.

## 📦 Распространение

### GitHub Releases

1. Создайте release на GitHub
2. Загрузите собранные файлы
3. Автообновления будут работать автоматически

### Собственный сервер

Настройте `publish` в `package.json`:

```json
{
  "build": {
    "publish": {
      "provider": "generic",
      "url": "https://your-server.com/updates"
    }
  }
}
```

## 🔒 Безопасность

### Рекомендации

1. **contextIsolation: true** - включено
2. **nodeIntegration: false** - отключено
3. **webSecurity: true** - включено
4. **CSP headers** - настроены в веб-приложении

### Проверка сертификатов

Для production используйте код-подпись:
- Windows: Code Signing Certificate
- macOS: Apple Developer Certificate

## 🚀 Интеграция с Vercel

### Вариант 1: Загрузка с Vercel

```javascript
const startUrl = 'https://your-app.vercel.app';
```

### Вариант 2: Локальная сборка

```javascript
const startUrl = `file://${path.join(__dirname, '../web/dist/index.html')}`;
```

**Рекомендуется**: Использовать Vercel URL для автоматических обновлений веб-приложения.

## 📝 Структура проекта

```
electron/
├── main.js          # Главный процесс Electron
├── preload.js       # Preload скрипт
├── package.json     # Конфигурация и зависимости
├── icon.ico         # Иконка Windows
├── icon.icns        # Иконка macOS
└── icon.png         # Иконка Linux
```

## 🔧 Troubleshooting

### Проблема: Приложение не запускается

**Решение:**
- Проверьте, что веб-приложение собрано: `cd web && npm run build`
- Проверьте пути к файлам в `main.js`
- Проверьте логи в консоли

### Проблема: Белый экран

**Решение:**
- Проверьте URL в `startUrl`
- Откройте DevTools и проверьте ошибки
- Проверьте CORS настройки backend

### Проблема: Автообновления не работают

**Решение:**
- Проверьте настройки `publish` в `package.json`
- Убедитесь, что сервер обновлений доступен
- Проверьте логи автообновления

## 📚 Дополнительные ресурсы

- [Electron Documentation](https://www.electronjs.org/docs)
- [electron-builder Documentation](https://www.electron.build/)
- [Electron Security](https://www.electronjs.org/docs/tutorial/security)

---

**Версия**: 1.0  
**Последнее обновление**: 2024
