# ⚡ Быстрое исправление для Render

## 🔧 Что исправить:

### Build Command:
**Сейчас:** `server-go/ $ go build -o main`  
**Должно быть:** `go build -o main .`

### Start Command:
**Сейчас:** `server-go/ $ ./main`  
**Должно быть:** `./main`

---

## 📝 Правильные команды:

### Build Command:
```
go build -o main .
```

### Start Command:
```
./main
```

---

## ✅ Что уже правильно:

- ✅ Name: `safegramm`
- ✅ Language: `Go`
- ✅ Branch: `main`
- ✅ Root Directory: `server-go`

---

## 🌍 Опционально: Region

Можно изменить Region на:
- `Frankfurt (EU Central)` - ближе к России, быстрее

---

## 📋 Дальше:

После исправления команд:

1. Прокрутите вниз до **Environment Variables**
2. Добавьте:
   ```
   JWT_SECRET=ваш-секретный-ключ-32-символа
   PORT=8080
   WEBHOOK_URL=http://localhost:3000/webhook
   NODE_ENV=production
   ```

3. Создайте PostgreSQL базу отдельно (если еще не создана):
   - "New +" → "PostgreSQL" → Free
   - Скопируйте Internal Database URL
   - Добавьте как `DATABASE_URL` в Environment Variables

4. Нажмите **"Create Web Service"**

5. Дождитесь деплоя (3-5 минут)

---

## ✅ Готово!

После исправления команд всё должно работать!
