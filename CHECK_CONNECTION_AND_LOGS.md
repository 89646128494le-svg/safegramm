# ✅ Проверка подключения и получение логов

Инструкция как проверить что всё работает и получать логи.

---

## 🔍 1. Проверка подключения

### Способ 1: Через браузер (самый простой)

1. Откройте ваш сайт на Vercel
2. Откройте **DevTools** (F12)
3. Перейдите на вкладку **Console**
4. Вы должны увидеть что приложение загружается
5. Если есть ошибки - они будут видны здесь

### Способ 2: Проверка Health Endpoint

Откройте в браузере или через curl:

**Локально:**
```
http://localhost:8080/health
```

**Через туннель (LocalTunnel/ngrok):**
```
https://your-tunnel-url.loca.lt/health
```

**Должно вернуть:**
```json
{
  "status": "ok",
  "timestamp": {}
}
```

### Способ 3: Через Network Tab

1. Откройте сайт на Vercel
2. **F12** → **Network**
3. Попробуйте войти или выполнить действие
4. Проверьте что запросы идут на правильный URL
5. Проверьте статус ответов (должны быть 200 OK)

---

## 📊 2. Логи Backend (на вашем ПК)

### Способ 1: Консоль где запущен backend

Если запустили через `START_LOCAL_SERVER.bat`, логи выводятся прямо в консоль.

Вы увидите:
- ✅ Подключение к базе данных
- ✅ Запуск сервера
- ✅ Ошибки (если есть)
- ✅ Запросы к API

### Способ 2: Логи в файл

Сейчас логи выводятся только в консоль. Для сохранения в файл нужно немного изменить скрипт.

---

## 🔔 3. Webhook для логов (получение логов на ПК)

Вы можете настроить webhook чтобы логи с backend приходили на ваш ПК.

### Шаг 1: Запустите Webhook Receiver

**В отдельном окне PowerShell:**

```powershell
cd "C:\Users\Lev\Desktop\Проекты\SafeGram перезапуск\webhook-receiver"
npm start
```

Или через скрипт:
```
webhook-receiver\start-windows.bat
```

Вы увидите:
```
SafeGram Webhook Receiver запущен на порту 3000
Ожидание webhook на http://localhost:3000/webhook
```

### Шаг 2: Настройте Webhook URL в backend

**Вариант A: Через Admin панель**

1. Зайдите на ваш сайт Vercel
2. Войдите как admin/owner
3. Перейдите в **Admin** → **Webhook**
4. Введите URL: `http://localhost:3000/webhook`
5. Нажмите **Сохранить URL**
6. Нажмите **Тестовая отправка**

**Вариант B: Через .env файл**

Откройте `server-go/.env` и добавьте/обновите:
```env
WEBHOOK_URL=http://localhost:3000/webhook
```

Перезапустите backend.

### Шаг 3: Для внешнего доступа (если backend на VPS)

Если backend на внешнем сервере, нужно чтобы webhook receiver был доступен:

1. Запустите LocalTunnel для webhook receiver:
   ```powershell
   lt --port 3000
   ```
2. Скопируйте URL (например: `https://webhook-receiver.loca.lt`)
3. В Admin панели укажите этот URL вместо localhost

---

## 📝 4. Просмотр логов Frontend

### Браузер Console

1. Откройте сайт на Vercel
2. **F12** → **Console**
3. Все логи, ошибки, предупреждения будут здесь

### Фильтрация логов

В консоли браузера можно фильтровать:
- **Errors** (красные) - ошибки
- **Warnings** (желтые) - предупреждения
- **Info** (синие) - информация
- **Network** - сетевые запросы

---

## 🧪 5. Полная проверка подключения

### Тест 1: Health Check

```powershell
# Локально
Invoke-WebRequest http://localhost:8080/health

# Через туннель (замените на ваш URL)
Invoke-WebRequest https://your-tunnel-url.loca.lt/health
```

### Тест 2: API запрос

```powershell
# Проверка что API работает
Invoke-WebRequest -Uri "https://your-tunnel-url.loca.lt/api/users/me" -Headers @{Authorization="Bearer your-token"}
```

### Тест 3: В браузере

1. Откройте ваш сайт
2. Попробуйте залогиниться
3. Проверьте что данные загружаются
4. Проверьте Console на ошибки

---

## 🔧 6. Настройка расширенного логирования

### Backend логи

В `server-go/.env` можно настроить:

```env
NODE_ENV=production
WEBHOOK_URL=http://localhost:3000/webhook
```

Логи автоматически отправляются на webhook если он настроен.

### Frontend логи

Логи frontend выводятся в консоль браузера автоматически.

Для production можно отключить подробные логи (но оставить ошибки).

---

## 📋 7. Чеклист проверки

- [ ] Backend запущен и отвечает на `/health`
- [ ] Frontend открывается и загружается
- [ ] В Console браузера нет критических ошибок
- [ ] API запросы идут на правильный URL (проверить в Network)
- [ ] Можно войти в систему
- [ ] Webhook receiver работает (если настроен)
- [ ] Логи приходят на webhook (если настроен)

---

## 🆘 8. Типичные проблемы

### Проблема: Backend не отвечает

**Проверьте:**
1. Запущен ли backend? (`START_LOCAL_SERVER.bat`)
2. Работает ли туннель? (LocalTunnel/ngrok)
3. Правильный ли URL в Vercel?

**Решение:**
- Проверьте что backend запущен
- Проверьте URL в Vercel Environment Variables
- Перезапустите туннель если нужно

### Проблема: CORS ошибки

**Ошибка:** `Access to fetch at '...' from origin '...' has been blocked by CORS policy`

**Решение:**
- Проверьте что `ALLOWED_ORIGINS` в backend включает ваш Vercel домен
- Или установите `ALLOWED_ORIGINS=*` для тестирования

### Проблема: 404 Not Found

**Ошибка:** `GET .../api/... 404`

**Решение:**
- Проверьте что URL правильный (без лишних слешей)
- Проверьте что backend запущен
- Проверьте роуты в backend

---

## ✅ Готово!

Теперь вы знаете как проверить подключение и получать логи!

---

## 📚 Дополнительно

- **Webhook система:** `WEBHOOK_SYSTEM.md`
- **Troubleshooting:** Проверьте Console и Network в браузере
- **Backend логи:** Смотрите в консоль где запущен backend
