# 🆓 Бесплатные хостинги для SafeGram Backend

## 🏆 Топ рекомендаций (2024)

### 1. Railway.app ⭐ РЕКОМЕНДУЕТСЯ

**Плюсы:**
- ✅ Очень простой деплой (1 клик из GitHub)
- ✅ Бесплатный план: $5 кредитов/месяц
- ✅ Автоматический PostgreSQL встроен
- ✅ Автоматический деплой при git push
- ✅ Хорошая документация
- ✅ SSL сертификаты автоматически

**Минусы:**
- ❌ После использования $5 приложение засыпает
- ❌ Медленный старт после сна (cold start)

**Что включено бесплатно:**
- 500 часов работы/месяц
- 5GB исходящего трафика
- 1GB дискового пространства
- PostgreSQL база данных

**Как деплоить:**

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите репозиторий `safegramm`
5. Укажите Root Directory: `server-go`
6. Railway автоматически определит Go проект
7. Добавьте переменные окружения:
   ```
   DATABASE_URL=postgresql://... (создается автоматически)
   JWT_SECRET=ваш-секретный-ключ
   PORT=8080
   WEBHOOK_URL=http://localhost:3000/webhook
   ```
8. Готово! Сервер задеплоится автоматически

**URL будет:** `https://your-app-name.up.railway.app`

---

### 2. Render.com ⭐ ВТОРОЙ ВЫБОР

**Плюсы:**
- ✅ Бесплатный план (с ограничениями)
- ✅ PostgreSQL база бесплатно
- ✅ SSL автоматически
- ✅ Простой деплой из GitHub
- ✅ Деплой при git push

**Минусы:**
- ❌ Приложение засыпает после 15 минут бездействия
- ❌ Долгий старт после сна (до 30 секунд)
- ❌ Медленнее чем Railway

**Что включено бесплатно:**
- 750 часов работы/месяц
- PostgreSQL база данных
- SSL сертификаты
- Автоматический деплой

**Как деплоить:**

1. Зайдите на https://render.com
2. Войдите через GitHub
3. Нажмите "New +" → "Web Service"
4. Подключите репозиторий `safegramm`
5. Настройки:
   - **Name:** `safegram-backend`
   - **Root Directory:** `server-go`
   - **Environment:** `Go`
   - **Build Command:** `go build -o main .`
   - **Start Command:** `./main`
   - **Plan:** `Free`
6. Добавьте переменные окружения (см. Railway)
7. Создайте PostgreSQL базу: "New +" → "PostgreSQL" → Free

**URL будет:** `https://safegram-backend.onrender.com`

---

### 3. Fly.io ⭐ ДЛЯ ПРОДАКШЕНА

**Плюсы:**
- ✅ Бесплатный план с хорошими лимитами
- ✅ Быстрый (edge network)
- ✅ Приложение не засыпает
- ✅ Глобальная сеть (ближе к пользователям)
- ✅ PostgreSQL база данных

**Минусы:**
- ❌ Более сложная настройка (нужен fly.toml)
- ❌ Нужен CLI для деплоя

**Что включено бесплатно:**
- 3 shared-cpu-1x виртуальные машины
- 160GB исходящего трафика/месяц
- 3GB дискового пространства
- PostgreSQL база данных

**Как деплоить:**

1. Установите Fly CLI:
   ```bash
   # Windows (PowerShell)
   iwr https://fly.io/install.ps1 -useb | iex
   
   # Или через winget
   winget install -e --id Fly.CLI
   ```

2. Войдите в Fly:
   ```bash
   fly auth login
   ```

3. Перейдите в директорию backend:
   ```bash
   cd server-go
   ```

4. Инициализируйте приложение:
   ```bash
   fly launch
   ```
   
   При запросе:
   - App name: `safegram-backend` (или любой)
   - Region: выберите ближайший (например, `iad` для США)
   - PostgreSQL: `Yes` (создаст базу автоматически)
   - Redis: `No` (опционально)

5. Добавьте переменные окружения:
   ```bash
   fly secrets set JWT_SECRET=ваш-секретный-ключ
   fly secrets set WEBHOOK_URL=http://localhost:3000/webhook
   ```

6. Задеплойте:
   ```bash
   fly deploy
   ```

**URL будет:** `https://safegram-backend.fly.dev`

---

### 4. DigitalOcean App Platform

**Плюсы:**
- ✅ Простой деплой
- ✅ Хорошая производительность
- ✅ PostgreSQL база данных

**Минусы:**
- ❌ Бесплатный план: только $200 кредитов на 60 дней (после платно)
- ❌ Дороже после истечения кредитов

**Что включено:**
- $200 кредитов на 60 дней (новичкам)
- Потом от $5/месяц

---

### 5. VPS (Oracle Cloud Free Tier) 🆓 НАВСЕГДА БЕСПЛАТНО

**Плюсы:**
- ✅ БЕСПЛАТНО НАВСЕГДА (не trial)
- ✅ 2 виртуальные машины (AMD или ARM)
- ✅ 200GB диск
- ✅ 10TB исходящего трафика/месяц
- ✅ Полный контроль

**Минусы:**
- ❌ Нужно настраивать вручную (Ubuntu, Docker, etc.)
- ❌ Более сложная настройка
- ❌ Регистрация может занять время

**Что включено бесплатно:**
- 2 VM.Standard.A1.Flex (4 CPU, 24GB RAM) или
- 2 VM.Standard.E2.1.Micro (1 CPU, 1GB RAM)
- 200GB дискового пространства
- 10TB трафика/месяц

**Как деплоить:**

1. Зарегистрируйтесь на https://cloud.oracle.com
2. Создайте бесплатный аккаунт (нужна кредитная карта, но не списывают)
3. Создайте VM (Always Free):
   - OS: Ubuntu 22.04
   - Shape: VM.Standard.A1.Flex (ARM)
   - SSH ключ: сгенерируйте новый
4. Подключитесь по SSH:
   ```bash
   ssh ubuntu@your-vm-ip
   ```
5. Установите Docker:
   ```bash
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh
   sudo usermod -aG docker ubuntu
   ```
6. Установите PostgreSQL:
   ```bash
   sudo apt update
   sudo apt install -y postgresql postgresql-contrib
   sudo systemctl start postgresql
   sudo systemctl enable postgresql
   ```
7. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/89646128494le-svg/safegramm.git
   cd safegramm/server-go
   ```
8. Создайте `.env`:
   ```bash
   nano .env
   ```
   ```
   DATABASE_URL=postgres://postgres:password@localhost:5432/safegram?sslmode=disable
   JWT_SECRET=ваш-секретный-ключ
   PORT=8080
   WEBHOOK_URL=http://localhost:3000/webhook
   ```
9. Запустите:
   ```bash
   go mod download
   go build -o main .
   ./main
   ```

Или через Docker (рекомендуется):
```bash
docker-compose up -d
```

---

## 📊 Сравнение

| Платформа | Простота | Бесплатно | Sleep | Скорость |
|-----------|----------|-----------|-------|----------|
| **Railway** | ⭐⭐⭐⭐⭐ | $5/мес | Да | ⭐⭐⭐⭐ |
| **Render** | ⭐⭐⭐⭐ | Да | Да | ⭐⭐⭐ |
| **Fly.io** | ⭐⭐⭐ | Да | Нет | ⭐⭐⭐⭐⭐ |
| **Oracle Cloud** | ⭐⭐ | Навсегда | Нет | ⭐⭐⭐⭐ |

---

## 🎯 Мои рекомендации

### Для начала (быстрый старт):
**Railway.app** - самый простой, за 5 минут задеплоите

### Для разработки:
**Render.com** - бесплатно, просто, но медленный старт

### Для продакшена:
**Fly.io** - быстрый, не засыпает, хорошая производительность

### Навсегда бесплатно:
**Oracle Cloud Free Tier** - если не боитесь настраивать вручную

---

## 🚀 Быстрый старт (Railway - РЕКОМЕНДУЕТСЯ)

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. "New Project" → "Deploy from GitHub repo"
4. Выберите `safegramm`
5. Root Directory: `server-go`
6. Добавьте переменные:
   ```
   JWT_SECRET=сгенерируйте-случайную-строку-32-символа
   PORT=8080
   WEBHOOK_URL=http://localhost:3000/webhook
   ```
7. PostgreSQL создастся автоматически
8. Готово! URL будет вида: `https://your-app.up.railway.app`

**Обновите `VITE_API_URL` во frontend на Vercel:**
```
VITE_API_URL=https://your-app.up.railway.app
```

---

## 🔐 Генерация JWT Secret

```bash
# Windows PowerShell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})

# Linux/Mac
openssl rand -base64 32
```

---

## ✅ Что дальше?

1. Задеплойте backend на Railway/Render/Fly
2. Получите URL (например: `https://safegram-backend.railway.app`)
3. Обновите `VITE_API_URL` в Vercel:
   - Settings → Environment Variables
   - `VITE_API_URL=https://your-backend-url.com`
4. Перезапустите деплой frontend
5. Готово! Мессенджер работает! 🎉
