# 🚂 Деплой SafeGram Backend на Railway

Пошаговая инструкция деплоя Go backend на Railway.

## 🚀 Быстрый деплой (5 минут)

### Шаг 1: Регистрация

1. Зайдите на https://railway.app
2. Нажмите "Login" → выберите "Login with GitHub"
3. Авторизуйте Railway доступ к GitHub

### Шаг 2: Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите репозиторий `89646128494le-svg/safegramm`
4. Нажмите **"Deploy Now"**

### Шаг 3: Настройка сервиса

1. Railway автоматически определит проект
2. Откройте **Settings** → **Root Directory**
3. Укажите: `server-go`
4. Сохраните

### Шаг 4: Добавление PostgreSQL

1. В проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. База создастся автоматически

### Шаг 5: Переменные окружения

1. Откройте сервис backend
2. Перейдите в **Variables**
3. Добавьте:

```
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
PORT=8080
WEBHOOK_URL=http://localhost:3000/webhook
NODE_ENV=production
```

**DATABASE_URL** добавится автоматически от PostgreSQL.

**Генерация JWT_SECRET:**

```powershell
# Windows PowerShell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

### Шаг 6: Деплой

1. Railway автоматически начнет деплой
2. Дождитесь завершения (2-3 минуты)
3. Откройте **Settings** → **Networking**
4. Нажмите **"Generate Domain"**
5. Скопируйте URL (например: `https://safegram-backend.up.railway.app`)

### Шаг 7: Проверка

Откройте в браузере:
```
https://your-app.up.railway.app/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":{}}
```

### Шаг 8: Обновление Frontend

1. Зайдите в Vercel Dashboard
2. Settings → Environment Variables
3. Добавьте/обновите:
   ```
   VITE_API_URL=https://your-app.up.railway.app
   ```
4. Перезапустите деплой

## ✅ Готово!

Backend задеплоен и готов к работе!

---

## 🔧 Дополнительные настройки

### CORS

Railway автоматически создаст домен. Добавьте его в `ALLOWED_ORIGINS`:

```
ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-app.up.railway.app
```

Или через Railway Variables.

### Автоматический деплой

По умолчанию Railway автоматически деплоит при push в `main` branch.

### Логи

Просмотр логов:
1. Откройте сервис в Railway
2. Вкладка **"Deployments"**
3. Нажмите на последний деплой
4. Видите логи в реальном времени

---

## 💰 Бесплатный план

Railway дает $5 кредитов/месяц бесплатно. Этого достаточно для:
- Backend сервера (24/7)
- PostgreSQL базы данных
- ~500 часов работы

После использования кредитов приложение засыпает, но можно разбудить вручную.

---

## 📚 Дополнительно

- Полная документация: Railway Docs
- Troubleshooting: Railway Dashboard → Logs
