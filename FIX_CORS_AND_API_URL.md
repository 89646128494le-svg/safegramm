# 🔧 Исправление CORS и VITE_API_URL

## ❌ Проблемы

1. **Frontend обращается к `localhost:8080`** вместо URL туннеля
2. **CORS блокирует запросы** с Vercel домена

---

## ✅ Решение

### Шаг 1: Обновите VITE_API_URL в Vercel

**ГЛАВНОЕ!** Frontend все еще использует `localhost:8080` вместо вашего туннеля.

1. Зайдите в **Vercel Dashboard**
2. Ваш проект → **Settings** → **Environment Variables**
3. Найдите `VITE_API_URL` или создайте новую переменную:
   - **Key:** `VITE_API_URL`
   - **Value:** Ваш URL туннеля (например: `https://random-name.loca.lt`)
   - Выберите все окружения: **Production**, **Preview**, **Development**
4. Нажмите **Save**
5. **ВАЖНО:** Перезапустите деплой:
   - **Deployments** → последний деплой → **"..."** → **Redeploy**

### Шаг 2: CORS уже исправлен

Я обновил код backend - теперь он автоматически разрешает все Vercel домены (`.vercel.app`, `.vercel-dns.com`).

**Перезапустите backend:**
```
START_LOCAL_SERVER.bat
```

---

## 🔍 Проверка

### 1. Проверьте что VITE_API_URL обновлен:

После обновления и передеплоя:
1. Откройте ваш сайт на Vercel
2. **F12** → **Network**
3. Попробуйте войти
4. Проверьте что запросы идут на **ваш туннель URL**, а НЕ на `localhost:8080`

### 2. Проверьте CORS:

В **Network** табе:
- Запросы должны возвращать статус **200 OK** (не CORS ошибки)
- Headers ответа должны содержать `Access-Control-Allow-Origin`

---

## 📋 Полная последовательность

1. ✅ **Обновите VITE_API_URL** в Vercel на ваш туннель URL
2. ✅ **Redeploy** на Vercel
3. ✅ **Перезапустите backend** (если нужно)
4. ✅ **Проверьте** что запросы идут на правильный URL

---

## ⚠️ Важно

- **URL туннеля меняется** при каждом перезапуске LocalTunnel
- Каждый раз когда перезапускаете туннель, нужно **обновлять VITE_API_URL** в Vercel
- Для постоянного URL используйте Cloudflare Tunnel с именем

---

## 🆘 Если все равно не работает

### Проблема: Все еще идет на localhost:8080

**Решение:**
- Убедитесь что обновили `VITE_API_URL` в Vercel
- Убедитесь что **перезапустили деплой** после обновления
- Очистите кэш браузера (Ctrl+Shift+R)

### Проблема: CORS ошибка все еще есть

**Решение:**
- Убедитесь что backend перезапущен (чтобы применить изменения CORS)
- Проверьте что запросы идут на правильный URL (не localhost)
- Проверьте что туннель работает: откройте `https://your-tunnel-url/health`

---

## ✅ После исправления

Вы должны увидеть:
- ✅ Запросы идут на ваш туннель URL
- ✅ Нет CORS ошибок
- ✅ Успешные ответы от API (200 OK)
- ✅ Можно войти в систему
