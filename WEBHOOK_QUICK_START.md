# ⚡ Быстрый старт: Webhook система

## 🎯 За 5 минут

### 1. Запустите Webhook Receiver

```bash
cd webhook-receiver
npm install
npm start
```

**Или используйте start.bat на Windows:**
```bash
start.bat
```

Receiver запустится на `http://localhost:3000`

### 2. Настройте Webhook в админ-панели

1. Войдите в SafeGram как **admin/owner**
2. Перейдите в `/app/admin`
3. Откройте вкладку **"🔗 Webhook"**
4. Введите URL: `http://localhost:3000/webhook`
5. Нажмите **"Сохранить"**
6. Нажмите **"Тест"** для проверки

### 3. Готово! ✅

Теперь все логи, ошибки и события от SafeGram сервера будут приходить на ваш ПК!

## 📋 Что вы увидите

- **Логи в консоли** - цветной вывод с уровнями (info, warning, error)
- **Логи в файлах** - `webhook-receiver/logs/safegram-YYYY-MM-DD.log`
- **Real-time уведомления** - все события сервера мгновенно

## 🔧 Для внешнего сервера

Если ваш backend в облаке:

1. Используйте **ngrok**: `ngrok http 3000`
2. Скопируйте полученный URL (например: `https://xxxx.ngrok.io`)
3. В админ-панели укажите: `https://xxxx.ngrok.io/webhook`
4. Или установите `WEBHOOK_URL` в переменных окружения сервера

## 📚 Подробная документация

- `WEBHOOK_SYSTEM.md` - полная документация
- `DEPLOY_BACKEND.md` - деплой backend сервера
