# 📧 Руководство: Управление сообщениями администрацией

## ✨ Что добавлено:

### 1. **Персональные письма от администрации**
- Администраторы могут отправлять персональные email пользователям
- Красивый дизайн в стиле SafeGram
- Опциональная кнопка с действием

### 2. **Уведомления о технических работах**
- Баннер вверху экрана для всех пользователей
- Автоматическое отображение при активации режима
- Возможность закрыть баннер (сохраняется в localStorage)
- Опциональная email-рассылка всем пользователям

---

## 🎯 Как использовать:

### Отправка персонального письма:

1. Откройте **Панель управления** (Admin Panel)
2. Перейдите на таб **"📧 Письма & Тех.работы"**
3. Выберите **"Персональные письма"**
4. Заполните форму:
   - **ID пользователя** (обязательно)
   - **Сообщение** (обязательно)
   - **Текст кнопки** (опционально) - например: "Открыть профиль"
   - **Ссылка кнопки** (опционально) - например: "https://safegram.app/profile"
5. Нажмите **"Отправить письмо"**

### Активация технических работ:

1. Откройте **Панель управления** (Admin Panel)
2. Перейдите на таб **"📧 Письма & Тех.работы"**
3. Выберите **"Технические работы"**
4. Заполните форму:
   - **Время проведения работ** - например: "25 января 2024, с 02:00 до 04:00 (МСК)"
   - **Описание работ** - например: "Во время работ доступ к сервису может быть ограничен"
   - **✓ Отправить email всем пользователям** (опционально)
5. Нажмите **"Активировать"**

**Результат:**
- ⚠️ Все пользователи увидят **жёлтый баннер вверху экрана**
- 📧 (Опционально) Всем отправятся email-уведомления

### Отключение режима технических работ:

1. В той же форме нажмите **"Отключить"**
2. Баннер исчезнет у всех пользователей

---

## 📧 Шаблоны писем:

### 1. Персональное письмо от администрации

**Тема:** "Сообщение от администрации SafeGram"

**Содержание:**
- Приветствие с именем пользователя
- Персональное сообщение
- Опциональная кнопка с действием
- Ссылка на форму обратной связи

**Пример:**
```
Здравствуйте, Иван!

У нас есть для вас важное сообщение:

┌──────────────────────────────────┐
│ Ваш аккаунт был активирован для  │
│ участия в бета-тестировании      │
│ новых функций!                   │
└──────────────────────────────────┘

[Открыть профиль] ← опциональная кнопка
```

### 2. Уведомление о технических работах

**Тема:** "⚠️ Плановые технические работы SafeGram"

**Содержание:**
- Информация о времени проведения работ
- Описание что будет недоступно
- Извинения за неудобства

**Пример:**
```
Здравствуйте, Иван!

Уведомляем вас о запланированных технических работах.

⏰ Время проведения работ:
   25 января 2024, с 02:00 до 04:00 (МСК)

ℹ️ Что это значит:
   Во время работ доступ к сервису может быть 
   ограничен или временно недоступен.
```

---

## 🎨 Баннер о технических работах:

### Дизайн:
- 🟡 **Жёлтый градиент** (orange) для привлечения внимания
- ⚠️ **Иконка предупреждения** с анимацией
- ⏰ **Время работ** рядом с описанием
- ❌ **Кнопка закрытия** в правом углу

### Поведение:
- Появляется **сверху экрана** с плавной анимацией
- Видно на **всех страницах** приложения
- **Можно закрыть** - состояние сохраняется в localStorage
- **Автоматически проверяет** статус каждые 5 минут
- При новой активации (новый ID) **появляется снова**

### Где отображается:
- ✅ В веб-приложении
- ✅ В мессенджере
- ✅ На всех страницах (главная, чаты, настройки и т.д.)

---

## 🔧 API Endpoints:

### Отправка персонального письма:
```http
POST /api/admin/send-email
Authorization: Bearer <admin-token>

{
  "userId": "user-id",
  "message": "Текст сообщения",
  "actionText": "Открыть",  // опционально
  "actionLink": "https://..."  // опционально
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Email sent successfully",
  "to": "user@example.com"
}
```

### Рассылка нескольким пользователям:
```http
POST /api/admin/broadcast-email
Authorization: Bearer <admin-token>

{
  "userIds": ["user-id-1", "user-id-2"],
  "message": "Текст сообщения",
  "actionText": "Открыть",
  "actionLink": "https://..."
}
```

**Ответ:**
```json
{
  "success": true,
  "successCount": 2,
  "failedCount": 0,
  "errors": []
}
```

### Активация технических работ:
```http
POST /api/admin/maintenance
Authorization: Bearer <admin-token>

{
  "timestamp": "25 января 2024, с 02:00 до 04:00 (МСК)",
  "message": "Во время работ доступ к сервису может быть ограничен",
  "sendEmail": true  // отправить email всем пользователям
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Maintenance mode activated",
  "emailsSent": 150,
  "emailsFailed": 5
}
```

### Проверка статуса технических работ (публичный):
```http
GET /api/maintenance/status

{
  "isActive": true,
  "timestamp": "25 января 2024, с 02:00 до 04:00 (МСК)",
  "message": "Во время работ доступ к сервису может быть ограничен",
  "id": "maintenance-id"
}
```

### Отключение технических работ:
```http
POST /api/admin/maintenance/disable
Authorization: Bearer <admin-token>

{
  "success": true,
  "message": "Maintenance mode disabled"
}
```

---

## 📦 Созданные файлы:

### Backend (Go):
1. **`server-go/internal/email/templates.go`** (обновлён)
   - `TemplateAdminMessage()` - шаблон персонального письма
   - `TemplateMaintenanceNotification()` - шаблон уведомления о тех. работах

2. **`server-go/internal/email/email.go`** (обновлён)
   - `SendAdminMessage()` - отправка персонального письма
   - `SendMaintenanceNotification()` - отправка уведомления о тех. работах

3. **`server-go/internal/models/maintenance.go`** (новый)
   - Модель `MaintenanceMode` для хранения статуса

4. **`server-go/internal/api/admin_messages.go`** (новый)
   - `SendPersonalEmail()` - endpoint для отправки письма
   - `BroadcastPersonalEmail()` - рассылка нескольким пользователям
   - `SendMaintenanceNotificationToAll()` - активация тех. работ
   - `GetMaintenanceStatus()` - получение статуса
   - `DisableMaintenance()` - отключение режима

5. **`server-go/internal/api/routes.go`** (обновлён)
   - Добавлены новые endpoints

### Frontend (React):
1. **`web/src/components/MaintenanceBanner.tsx`** (новый)
   - Баннер о технических работах

2. **`web/src/components/admin/AdminMessaging.tsx`** (новый)
   - Интерфейс для управления письмами и тех. работами

3. **`web/src/pages/AppShell.tsx`** (обновлён)
   - Добавлен `<MaintenanceBanner />`

4. **`web/src/pages/admin/Admin.tsx`** (обновлён)
   - Добавлен таб "📧 Письма & Тех.работы"

5. **`web/src/components/admin/index.ts`** (обновлён)
   - Экспорт `AdminMessaging`

---

## 🧪 Тестирование:

### 1. Тест персонального письма:

```bash
# Отправить письмо пользователю
curl -X POST http://localhost:8080/api/admin/send-email \
  -H "Authorization: Bearer <admin-token>" \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "user-id",
    "message": "Тестовое сообщение от администрации",
    "actionText": "Открыть",
    "actionLink": "https://safegram.app"
  }'
```

### 2. Тест технических работ:

```bash
# Активировать режим технических работ
curl -X POST http://localhost:8080/api/admin/maintenance \
  -H "Authorization: Bearer <admin-token>" \
  -H "Content-Type: application/json" \
  -d '{
    "timestamp": "Сегодня, с 22:00 до 23:00",
    "message": "Обновление базы данных",
    "sendEmail": false
  }'

# Проверить статус (без авторизации)
curl http://localhost:8080/api/maintenance/status

# Отключить режим
curl -X POST http://localhost:8080/api/admin/maintenance/disable \
  -H "Authorization: Bearer <admin-token>"
```

### 3. Тест в браузере:

**Персональное письмо:**
1. Войдите как администратор
2. Откройте "Панель управления"
3. Таб "📧 Письма & Тех.работы" → "Персональные письма"
4. Заполните форму и отправьте
5. Проверьте email получателя

**Технические работы:**
1. В той же вкладке переключитесь на "Технические работы"
2. Заполните форму
3. Нажмите "Активировать"
4. **Откройте приложение в другой вкладке/браузере**
5. ✅ Должен появиться **жёлтый баннер вверху**
6. Закройте баннер крестиком - он исчезнет
7. Обновите страницу - баннер **НЕ появится** снова (сохранено в localStorage)

---

## 🎨 Визуальный дизайн:

### Персональное письмо:
```
╔══════════════════════════════════════╗
║        SafeGram (градиент)           ║
╠══════════════════════════════════════╣
║ 📨 Сообщение от администрации        ║
║                                      ║
║ Здравствуйте, Иван!                  ║
║                                      ║
║ ┌────────────────────────────────┐   ║
║ │ Персональное сообщение здесь   │   ║
║ └────────────────────────────────┘   ║
║                                      ║
║       [Кнопка действия]              ║
║                                      ║
║ ────────────────────────────────     ║
║                                      ║
║ SafeGram - Безопасный мессенджер     ║
╚══════════════════════════════════════╝
```

### Баннер о технических работах:
```
┌────────────────────────────────────────────────────────┐
│ ⚠️ Плановые технические работы                      ❌ │
│                                                         │
│ ⏰ Время: 25 января, с 02:00 до 04:00 (МСК)            │
│ ℹ️ Во время работ доступ может быть ограничен          │
└────────────────────────────────────────────────────────┘
```

---

## 🔐 Безопасность:

### Требования доступа:
- **Персональные письма:** Роль `admin` или `owner`
- **Технические работы:** Роль `admin` или `owner`
- **Просмотр статуса:** Все пользователи (публичный endpoint)

### Проверка ролей:
```go
// В каждом endpoint
hasAccess := false
for _, role := range user.Roles {
    if role == "admin" || role == "owner" {
        hasAccess = true
        break
    }
}
```

---

## 💾 База данных:

### Таблица MaintenanceMode:
```sql
CREATE TABLE maintenance_modes (
    id VARCHAR(255) PRIMARY KEY DEFAULT gen_random_uuid(),
    is_active BOOLEAN DEFAULT false,
    timestamp VARCHAR(255),  -- Текстовое описание времени
    message TEXT,            -- Описание работ
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_maintenance_active ON maintenance_modes(is_active);
```

---

## 📊 Примеры использования:

### Сценарий 1: Персональное письмо пользователю

**Ситуация:** Пользователь написал в поддержку, нужно отправить ответ

**Действия:**
1. Найдите ID пользователя в админ-панели (таб "Пользователи")
2. Перейдите на "Письма & Тех.работы"
3. Введите ID и сообщение:
   ```
   Здравствуйте! Мы проверили вашу проблему.
   Исправление будет выпущено в ближайшем обновлении.
   ```
4. Добавьте кнопку:
   - Текст: "Следить за обновлениями"
   - Ссылка: "https://safegram.app/changelog"
5. Отправьте

### Сценарий 2: Плановые технические работы

**Ситуация:** Нужно обновить сервер, требуется время простоя

**Действия:**
1. За день до работ:
   - Активируйте режим технических работ
   - Время: "26 января 2024, с 03:00 до 05:00 (МСК)"
   - Сообщение: "Будет проведено обновление серверов. Все функции будут временно недоступны."
   - ✓ Отправить email всем пользователям
   - Нажмите "Активировать"

2. Пользователи увидят:
   - Баннер на сайте
   - Email-уведомление

3. После завершения работ:
   - Нажмите "Отключить"
   - Баннер исчезнет

### Сценарий 3: Срочное техническое уведомление

**Ситуация:** Обнаружена проблема, нужно срочно уведомить

**Действия:**
1. Активируйте режим:
   - Время: "Прямо сейчас, ~30 минут"
   - Сообщение: "Обнаружена проблема, работаем над устранением"
   - ✓ Отправить email (чтобы все узнали)
2. После исправления - отключите режим

---

## ⚙️ Настройка:

### 1. Миграция базы данных:

После обновления кода запустите Go сервер - таблица создастся автоматически (через GORM AutoMigrate).

Или создайте вручную:
```bash
cd server-go
# Добавьте в main.go:
# db.AutoMigrate(&models.MaintenanceMode{})
```

### 2. Проверка email настроек:

Убедитесь что в `.env` настроен email:
```env
EMAIL_PROVIDER=gmail
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
EMAIL_FROM_NAME=SafeGram
```

---

## 🎉 Готово!

Теперь у администраторов есть:
- ✅ Возможность отправлять персональные письма пользователям
- ✅ Система управления техническими работами
- ✅ Красивые email-шаблоны
- ✅ Удобный интерфейс в админ-панели
- ✅ Глобальный баннер для всех пользователей

**Используйте ответственно! 🚀**

---

## 📝 Примечания:

1. **Email-рассылка всем** может занять время при большом количестве пользователей
2. **localStorage** используется для хранения состояния закрытого баннера (не сбрасывается)
3. **Новый ID** технических работ сбросит флаг "закрыто" и покажет баннер снова
4. **Баннер имеет z-index: 9998** - отображается поверх почти всего контента
5. **Персональные письма** не логируются в базу данных (только отправка через SMTP)

---

## 🆘 Troubleshooting:

**Баннер не появляется:**
- Проверьте `/api/maintenance/status` - должен вернуть `isActive: true`
- Очистите localStorage: `localStorage.removeItem('dismissedMaintenanceId')`
- Обновите страницу

**Письма не отправляются:**
- Проверьте настройки email в `.env`
- Проверьте логи Go сервера
- Убедитесь что у вас роль admin/owner

**Ошибка 403 Forbidden:**
- У вас нет прав администратора
- Проверьте роли: `/api/users/me`
