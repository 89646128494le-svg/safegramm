# 🔧 Устранение проблем с ngrok

## ✅ Токен установлен успешно

Вы уже выполнили:
```powershell
C:\ngrok\ngrok.exe authtoken 30YlL4Qa53rGSPmN6MQrZMd0YdL_XVY1ZySboAK7xLBALz18
```

Токен сохранен в: `C:\Users\Lev\AppData\Local\ngrok\ngrok.yml`

---

## 🚀 Как запустить ngrok

### Способ 1: Через скрипт

Просто запустите:
```
RUN_NGROK.bat
```

### Способ 2: Вручную (PowerShell)

```powershell
C:\ngrok\ngrok.exe http 8080
```

---

## ⚠️ Важно!

**Перед запуском ngrok убедитесь что:**

1. ✅ Backend запущен на порту 8080
   - Запустите `START_LOCAL_SERVER.bat` в отдельном окне
   - Должно быть: "Backend starting on port 8080"

2. ✅ Если backend не запущен, ngrok подключится, но будет показывать ошибку "502 Bad Gateway"

---

## 🔍 Проверка

### Проверить что backend работает:

Откройте в браузере:
```
http://localhost:8080/health
```

Должно вернуть: `{"status":"ok","timestamp":{...}}`

### Проверить что ngrok работает:

После запуска ngrok вы увидите:
```
Session Status                online
Account                       ваше-имя (Plan: Free)
Version                       3.x.x
Region                        United States (us)
Latency                       -
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123.ngrok-free.app -> http://localhost:8080
```

**Скопируйте URL:** `https://abc123.ngrok-free.app`

---

## 🐛 Проблемы и решения

### Проблема: ngrok не запускается

**Решение:**
1. Проверьте что файл существует: `C:\ngrok\ngrok.exe`
2. Попробуйте запустить вручную в PowerShell:
   ```powershell
   cd C:\ngrok
   .\ngrok.exe http 8080
   ```

### Проблема: "ngrok not found"

**Решение:**
- Убедитесь что ngrok.exe находится в `C:\ngrok\`
- Если нет, скачайте с https://ngrok.com/download
- Распакуйте архив в `C:\ngrok\`

### Проблема: "authtoken not found"

**Решение:**
- Токен уже установлен! ✅
- Если все равно показывает ошибку, попробуйте еще раз:
  ```powershell
  C:\ngrok\ngrok.exe authtoken 30YlL4Qa53rGSPmN6MQrZMd0YdL_XVY1ZySboAK7xLBALz18
  ```

### Проблема: "502 Bad Gateway" в ngrok

**Решение:**
- Backend не запущен на порту 8080
- Запустите `START_LOCAL_SERVER.bat` сначала

### Проблема: ngrok показывает "ERR_NGROK_108"

**Решение:**
- Это ошибка авторизации
- Проверьте токен: `C:\ngrok\ngrok.exe config check`
- Если ошибка, переустановите токен

---

## 📋 Полная последовательность действий

1. **Откройте первое окно PowerShell/CMD:**
   ```
   START_LOCAL_SERVER.bat
   ```
   Дождитесь сообщения: "Backend starting on port 8080"

2. **Откройте второе окно PowerShell/CMD:**
   ```
   RUN_NGROK.bat
   ```
   Или вручную:
   ```powershell
   C:\ngrok\ngrok.exe http 8080
   ```

3. **Скопируйте ngrok URL** (например: `https://abc123.ngrok-free.app`)

4. **Обновите Vercel:**
   - Dashboard → Settings → Environment Variables
   - `VITE_API_URL` = ваш ngrok URL
   - Redeploy

---

## 🌐 Альтернатива: Cloudflare Tunnel

Если ngrok продолжает вызывать проблемы, используйте Cloudflare Tunnel:

1. Скачайте: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/
2. Распакуйте в `C:\cloudflared\`
3. Запустите: `SETUP_CLOUDFLARE_TUNNEL.bat`

**Преимущества:**
- ✅ Постоянный URL (не меняется)
- ✅ Полностью бесплатно
- ✅ Без регистрации для быстрого режима

---

## ✅ Готово!

После запуска ngrok вы получите публичный URL для вашего backend!
