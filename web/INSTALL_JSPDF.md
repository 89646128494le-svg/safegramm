# Установка jspdf

Для работы экспорта в PDF нужно установить зависимость jspdf.

Выполните в терминале:
```bash
cd web
npm install
```

Если jspdf все еще не установлен, выполните:
```bash
npm install jspdf
```
