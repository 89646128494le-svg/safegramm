# 🔧 Исправление ошибки сборки на Vercel

## Проблема

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@vitejs/plugin-react'
```

Vite не может найти плагин React, который используется в `vite.config.js`, но отсутствует в `package.json`.

## ✅ Решение (применено)

Добавлен `@vitejs/plugin-react` в `devDependencies` файла `web/package.json`:

```json
"devDependencies": {
  "@vitejs/plugin-react": "^4.2.1",
  ...
}
```

## 🚀 Что делать дальше

1. **Закоммитьте изменения:**
   ```bash
   git add web/package.json
   git commit -m "Add missing @vitejs/plugin-react dependency"
   git push
   ```

2. **Vercel автоматически пересоберет проект**

3. **Проверьте логи** - теперь должно быть:
   - ✅ Установлен `@vitejs/plugin-react`
   - ✅ Vite config загружается успешно
   - ✅ Сборка проходит без ошибок

## 📝 Проверка локально

Перед push можно проверить локально:

```bash
cd web
npm install
npm run build
```

Если сборка проходит успешно локально, то и на Vercel должно работать.

---

**Статус**: ✅ Исправлено - добавлен `@vitejs/plugin-react`
