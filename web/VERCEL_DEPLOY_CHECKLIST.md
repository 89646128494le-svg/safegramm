# ✅ Чеклист для исправления деплоя на Vercel

## Проблема
Vercel не может найти `@vitejs/plugin-react` во время сборки, хотя пакет добавлен в `package.json`.

## ✅ Проверка локальных файлов

1. **package.json** - содержит `@vitejs/plugin-react` в devDependencies ✅
2. **package-lock.json** - содержит `@vitejs/plugin-react` ✅
3. **vercel.json** - настроен `rootDirectory: "web"` ✅

## 🔧 Решение

### Шаг 1: Убедитесь, что все файлы закоммичены

```bash
git status
```

Должны быть закоммичены:
- `web/package.json`
- `web/package-lock.json`
- `vercel.json` (в корне)

Если есть незакоммиченные изменения:
```bash
git add web/package.json web/package-lock.json vercel.json
git commit -m "Fix Vercel build: add @vitejs/plugin-react and configure rootDirectory"
git push
```

### Шаг 2: Очистите кеш Vercel

В Vercel Dashboard:
1. Откройте ваш проект
2. Перейдите в **Settings** → **General**
3. Прокрутите вниз до **Build & Development Settings**
4. Нажмите **Clear Build Cache**
5. Или перейдите в **Deployments** → выберите последний деплой → **Redeploy** → включите **Use existing Build Cache** = OFF

### Шаг 3: Проверьте Root Directory в Vercel

В Vercel Dashboard:
1. **Settings** → **General**
2. Найдите **Root Directory**
3. Убедитесь, что установлено: `web`
4. Если нет - установите и сохраните

### Шаг 4: Пересоберите проект

После очистки кеша и проверки настроек:
- Vercel автоматически пересоберет при следующем push
- Или нажмите **Redeploy** в последнем деплое

## 🔍 Альтернативное решение: Явная команда установки

Если проблема сохраняется, можно явно указать установку зависимостей в `vercel.json`:

```json
{
  "installCommand": "npm ci"
}
```

Или добавить в `package.json` скрипт:
```json
{
  "scripts": {
    "vercel-build": "npm install && npm run build"
  }
}
```

И в `vercel.json`:
```json
{
  "buildCommand": "npm run vercel-build"
}
```

## 📝 Проверка после исправления

В логах Vercel должно быть:
- ✅ `added X packages` (много пакетов, не 48)
- ✅ `@vitejs/plugin-react` в списке установленных пакетов
- ✅ `vite build` выполняется успешно
- ✅ `dist/` создается с файлами

---

**Статус**: Локальные файлы корректны, нужно закоммитить и очистить кеш Vercel
