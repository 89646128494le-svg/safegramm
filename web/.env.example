# URL бэкенда (server-go). При запуске через START_ALL.bat API на порту 8082.
VITE_API_URL=http://localhost:8082

# Опционально: базовый URL CDN для статики и медиа (аватары, стикеры). Если не задан — используются те же домены.
# VITE_CDN_URL=https://cdn.example.com
