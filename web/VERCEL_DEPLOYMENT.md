# 🚀 Полная инструкция по деплою SafeGram на Vercel

## 📋 План деплоя

### Архитектура:
- **Vercel** - хостинг многостраничного сайта (Landing, Features, Pricing, About, etc.)
- **Backend API** - отдельный сервер (Go/Node.js) для API
- **База данных** - PostgreSQL (можно на Railway, Supabase, или VPS)
- **Redis** - для кэширования (можно на Upstash)
- **Telegram бот** - отдельный сервис

### Управление через админ-панель:
Админ-панель на Vercel будет управлять всеми сервисами через API.

---

## 🔧 Шаг 1: Подготовка проекта

### 1.1 Структура проекта

```
SafeGram/
├── web/                    # Frontend (деплой на Vercel)
│   ├── src/
│   │   ├── pages/          # Многостраничный сайт
│   │   ├── components/     # Компоненты
│   │   └── ...
│   ├── vercel.json         # Конфигурация Vercel
│   └── package.json
├── server-go/              # Backend API (отдельный сервер)
├── server/                 # Node.js альтернатива
└── vercel.json             # Корневая конфигурация
```

### 1.2 Настройка переменных окружения

В `web/.env` (для локальной разработки):
```env
VITE_API_URL=http://localhost:8080
VITE_GIPHY_API_KEY=your_key_here
```

В Vercel Dashboard → Settings → Environment Variables:
```
VITE_API_URL=https://your-api-domain.com
VITE_GIPHY_API_KEY=your_key_here
```

---

## 🌐 Шаг 2: Деплой на Vercel

### Вариант A: Через Vercel CLI

```bash
# 1. Установка Vercel CLI
npm i -g vercel

# 2. Логин
vercel login

# 3. Перейти в директорию web
cd web

# 4. Деплой
vercel

# 5. Production деплой
vercel --prod
```

### Вариант B: Через GitHub

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/safegram.git
   git push -u origin main
   ```

2. **Импортируйте в Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Импортируйте репозиторий
   - Настройки:
     - **Framework Preset**: Vite
     - **Root Directory**: `web`
     - **Build Command**: `npm run build`
     - **Output Directory**: `dist`
     - **Install Command**: `npm install`

3. **Добавьте переменные окружения:**
   - VITE_API_URL
   - VITE_GIPHY_API_KEY

4. **Деплой!**

---

## ⚙️ Шаг 3: Настройка Backend API

### 3.1 Деплой Backend на отдельный сервер

**Варианты:**
- **Railway** - легко деплоить Go/Node.js
- **Render** - бесплатный tier
- **DigitalOcean App Platform**
- **VPS** (DigitalOcean, Hetzner) с Docker

### 3.2 Пример для Railway:

```bash
cd server-go
railway login
railway init
railway up
```

### 3.3 Переменные окружения Backend:

```
DATABASE_URL=postgresql://...
REDIS_URL=redis://...
JWT_SECRET=your_secret
VAPID_PUBLIC_KEY=...
VAPID_PRIVATE_KEY=...
```

---

## 🗄️ Шаг 4: Настройка базы данных

### Вариант A: Supabase (бесплатный tier)

1. Создайте проект на [supabase.com](https://supabase.com)
2. Получите connection string
3. Добавьте в переменные окружения Backend

### Вариант B: Railway PostgreSQL

1. Создайте PostgreSQL в Railway
2. Получите DATABASE_URL
3. Добавьте в переменные окружения

### Вариант C: VPS с PostgreSQL

```bash
# На VPS
docker run -d \
  --name postgres \
  -e POSTGRES_PASSWORD=your_password \
  -e POSTGRES_DB=safegram \
  -p 5432:5432 \
  postgres:15
```

---

## 📡 Шаг 5: Настройка Redis

### Вариант A: Upstash (бесплатный tier)

1. Создайте Redis на [upstash.com](https://upstash.com)
2. Получите REDIS_URL
3. Добавьте в переменные окружения Backend

### Вариант B: Railway Redis

1. Создайте Redis в Railway
2. Получите REDIS_URL
3. Добавьте в переменные окружения

---

## 🤖 Шаг 6: Настройка Telegram бота

### 6.1 Создание бота

1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Создайте бота: `/newbot`
3. Получите токен

### 6.2 Деплой бота

**Вариант A: Railway**
```bash
cd telegram-bot  # если есть отдельная директория
railway up
```

**Вариант B: VPS**
```bash
pm2 start bot.js --name telegram-bot
```

---

## 🎛️ Шаг 7: Настройка управления сервисами

### 7.1 Backend API эндпоинты

Уже созданы в `server-go/internal/api/services.go`:
- `GET /api/admin/services` - статус всех сервисов
- `POST /api/admin/services/:id/start` - запуск сервиса
- `POST /api/admin/services/:id/stop` - остановка сервиса
- `POST /api/admin/services/:id/restart` - перезапуск сервиса

### 7.2 Интеграция с внешними API

**Для управления реальными сервисами нужно:**

1. **Vercel API** - для управления деплоями
   - Получить API токен в Vercel Settings
   - Интегрировать в `StartService/StopService`

2. **Docker API** - для управления контейнерами
   - Подключиться к Docker daemon
   - Использовать Docker SDK

3. **PM2 API** - для управления процессами
   - Установить PM2
   - Использовать PM2 API

4. **Кастомный оркестратор** - собственный сервис управления

### 7.3 Пример интеграции с PM2:

```go
// server-go/internal/api/services.go

import (
    "github.com/Unknwon/goconfig"
    "github.com/keybase/go-ps"
)

func startServicePM2(serviceID string) error {
    cmd := exec.Command("pm2", "start", serviceID, "--name", serviceID)
    return cmd.Run()
}
```

---

## 🎯 Шаг 8: Использование админ-панели

### 8.1 Доступ к админ-панели

1. Войдите в SafeGram
2. Убедитесь, что у вас есть роль `admin` или `owner`
3. Перейдите в `/app/admin`
4. Откройте вкладку "Сервисы"

### 8.2 Управление сервисами

В админ-панели вы можете:
- ✅ Просматривать статус всех сервисов
- ✅ Запускать сервисы
- ✅ Останавливать сервисы
- ✅ Перезапускать сервисы
- ✅ Видеть health check статус
- ✅ Автоматическое обновление статуса каждые 10 секунд

---

## 📊 Итоговая архитектура

```
┌─────────────────────────────────────┐
│   Vercel (Frontend + Landing)      │
│   safegram.vercel.app               │
│   - Landing                         │
│   - Features                        │
│   - Pricing                         │
│   - About                           │
│   - App (Chats)                     │
│   - Admin Panel                     │
└──────────────┬──────────────────────┘
               │
               │ API Calls
               │
┌──────────────▼──────────────────────┐
│   Backend API Server                │
│   api.safegram.com                  │
│   - REST API                        │
│   - WebSocket                       │
│   - Service Management API          │
└──────────────┬──────────────────────┘
               │
       ┌───────┴────────┐
       │                │
┌──────▼──────┐  ┌──────▼──────┐
│ PostgreSQL  │  │    Redis    │
│  Database   │  │    Cache    │
└─────────────┘  └─────────────┘
       │
┌──────▼──────┐
│ Telegram Bot│
│   Service   │
└─────────────┘
```

---

## ✅ Чек-лист деплоя

- [ ] Frontend задеплоен на Vercel
- [ ] Backend API задеплоен на сервер
- [ ] База данных PostgreSQL настроена
- [ ] Redis настроен
- [ ] Telegram бот запущен
- [ ] Переменные окружения настроены
- [ ] API эндпоинты для управления сервисами работают
- [ ] Админ-панель доступна и работает
- [ ] Health checks настроены
- [ ] SSL сертификаты работают

---

## 🚀 Быстрый старт

```bash
# 1. Деплой Frontend на Vercel
cd web
vercel --prod

# 2. Деплой Backend (Railway пример)
cd server-go
railway up

# 3. Настройка переменных окружения в Vercel Dashboard
# VITE_API_URL=https://your-api.railway.app

# 4. Готово!
# Откройте https://your-project.vercel.app
```

---

## 📝 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Railway Documentation](https://docs.railway.app)
- [Supabase Documentation](https://supabase.com/docs)
- [Upstash Documentation](https://docs.upstash.com)
