# 🚀 Деплой SafeGram на Vercel

## Быстрый деплой

### 1. Подготовка

```bash
cd web
npm install
npm run build
```

### 2. Деплой через Vercel CLI

```bash
# Установка Vercel CLI
npm i -g vercel

# Логин в Vercel
vercel login

# Деплой
vercel

# Production деплой
vercel --prod
```

### 3. Деплой через GitHub

1. Создайте репозиторий на GitHub
2. Закоммитьте код:
   ```bash
   git add .
   git commit -m "Initial commit"
   git push origin main
   ```
3. Перейдите на [vercel.com](https://vercel.com)
4. Импортируйте репозиторий
5. Настройки:
   - Framework Preset: Vite
   - Root Directory: `web`
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

### 4. Переменные окружения

В настройках проекта Vercel добавьте:

```
VITE_API_URL=https://your-api-domain.com
VITE_GIPHY_API_KEY=your_giphy_key
```

## 📝 Структура для Vercel

- `web/vercel.json` - конфигурация Vercel
- `web/.vercelignore` - игнорируемые файлы
- `vercel.json` - корневая конфигурация (если нужно)

## 🔧 Особенности

- ✅ Автоматический деплой при push в main
- ✅ Preview деплои для каждого PR
- ✅ Edge Network для быстрой загрузки
- ✅ HTTPS из коробки
- ✅ Автоматические SSL сертификаты

## ⚙️ Управление сервисами через админ-панель

После деплоя на Vercel, вы сможете управлять всеми сервисами через админ-панель:

1. Зайдите в `/app/admin`
2. Перейдите на вкладку "Сервисы"
3. Управляйте:
   - Веб-приложение (Vercel)
   - API сервер
   - База данных
   - Telegram бот
   - Redis кэш

## 🎯 Что нужно для полной интеграции

1. **Backend API** - нужны эндпоинты для управления сервисами:
   - `POST /api/admin/services/:id/start`
   - `POST /api/admin/services/:id/stop`
   - `POST /api/admin/services/:id/restart`
   - `GET /api/admin/services/status`

2. **Внешние сервисы** - для управления реальными сервисами:
   - Vercel API для управления деплоями
   - Docker API для управления контейнерами
   - PM2 API для управления процессами
   - Или кастомный оркестратор

## 📚 Дополнительная документация

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel CLI](https://vercel.com/docs/cli)
