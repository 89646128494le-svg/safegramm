# ⚡ Быстрое исправление CORS

## 🔴 Проблема

CORS блокирует запросы: `Access to fetch at 'https://my-safegram.loca.lt/api/auth/login' from origin 'https://safegram-hazel.vercel.app'`

---

## ✅ Решение (2 минуты)

### Шаг 1: Создайте/обновите .env файл

**Вариант A: Автоматически (РЕКОМЕНДУЕТСЯ)**

Запустите в папке `server-go`:
```
CREATE_ENV.bat
```

**Вариант B: Вручную**

1. Откройте `server-go/.env` (или создайте если нет)
2. Добавьте строку:
   ```env
   ALLOW_ALL_ORIGINS=true
   ```

### Шаг 2: Перезапустите backend

1. Остановите текущий backend (Ctrl+C в окне где он запущен)
2. Запустите снова:
   ```
   START_LOCAL_SERVER.bat
   ```

### Шаг 3: Проверьте

1. Откройте ваш сайт на Vercel
2. F12 → Network
3. Попробуйте войти
4. CORS ошибки должны исчезнуть ✅

---

## 🔍 Проверка туннеля

Также убедитесь что туннель работает:

```
https://my-safegram.loca.lt/health
```

Должно вернуть: `{"status":"ok","timestamp":{}}`

Если не работает - перезапустите туннель:
```
USELOCALTUNNEL.bat
```

---

## 📋 Полный .env пример

Если создаете вручную, вот пример:

```env
DATABASE_URL=postgres://safegram:safegram@localhost:5432/safegram?sslmode=disable
JWT_SECRET=dev-secret-change-in-production
PORT=8080
REDIS_URL=localhost:6379
WEBHOOK_URL=http://localhost:3000/webhook
NODE_ENV=development
ALLOW_ALL_ORIGINS=true
```

**Главное:** строка `ALLOW_ALL_ORIGINS=true` должна быть!

---

## ✅ Готово!

После этих шагов CORS должен работать. Если нет - проверьте что:
1. ✅ .env содержит `ALLOW_ALL_ORIGINS=true`
2. ✅ Backend перезапущен после изменения .env
3. ✅ Туннель работает (проверьте /health)
