# ✅ SafeGram готов к деплою на Vercel!

## 🎯 Текущий статус

✅ **Frontend готов к деплою на Vercel**
- Конфигурация `vercel.json` настроена
- Security headers добавлены
- SPA routing настроен
- Production build оптимизирован

## 🚀 Быстрый деплой (3 шага)

### Шаг 1: Установка Vercel CLI
```bash
npm i -g vercel
```

### Шаг 2: Логин
```bash
vercel login
```

### Шаг 3: Деплой
```bash
cd web
vercel --prod
```

**Готово!** Ваш сайт будет доступен по адресу `https://your-project.vercel.app`

## ⚙️ Настройка переменных окружения

В Vercel Dashboard → Settings → Environment Variables добавьте:

### Обязательные:
```
VITE_API_URL=https://your-backend-api.com
VITE_WS_URL=wss://your-backend-api.com
```

### Опциональные:
```
VITE_ENVIRONMENT=production
VITE_SENTRY_DSN=your-sentry-dsn
VITE_ANALYTICS_ID=your-analytics-id
```

## 📋 Чеклист перед деплоем

- [ ] Backend API развернут и доступен
- [ ] CORS настроен на backend для Vercel домена
- [ ] WebSocket сервер настроен (отдельно от Vercel)
- [ ] Переменные окружения добавлены в Vercel
- [ ] Кастомный домен настроен (опционально)

## 🔧 Настройка кастомного домена

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен (например, `safegram.com`)
3. Настройте DNS записи:
   - **A record**: `@` → IP адрес Vercel
   - **CNAME**: `www` → `cname.vercel-dns.com`
4. Vercel автоматически выдаст SSL сертификат

## ⚠️ Важные замечания

### Backend должен быть отдельно
Vercel - это только для frontend. Backend нужно развернуть отдельно:
- **Railway** - легко для Go/Node.js
- **Render** - бесплатный tier
- **DigitalOcean** - VPS с Docker
- **AWS/GCP** - для масштабирования

### WebSocket
Vercel Serverless Functions не поддерживают WebSocket. Нужен отдельный сервер для WebSocket соединений.

### CORS
Убедитесь, что backend разрешает запросы с вашего Vercel домена:
```
ALLOWED_ORIGINS=https://your-project.vercel.app,https://yourdomain.com
```

## 📊 Архитектура деплоя

```
┌─────────────────────────┐
│   Vercel (Frontend)     │
│   safegram.vercel.app   │
│   - React SPA           │
│   - Static files         │
└───────────┬─────────────┘
            │
            │ API Calls
            │
┌───────────▼─────────────┐
│   Backend API Server    │
│   api.safegram.com      │
│   - REST API            │
│   - WebSocket           │
└───────────┬─────────────┘
            │
    ┌───────┴───────┐
    │               │
┌───▼───┐      ┌───▼───┐
│  DB   │      │ Redis │
└───────┘      └───────┘
```

## 🎨 Production оптимизации

Все уже настроено в `vite.config.js`:
- ✅ Минификация кода
- ✅ Tree-shaking
- ✅ Code splitting
- ✅ Удаление console.log в production
- ✅ Оптимизация изображений

## 🔄 Автоматический деплой

### Через GitHub

1. Подключите репозиторий в Vercel
2. Настройте автоматический деплой:
   - **Production**: при push в `main`/`master`
   - **Preview**: при создании PR

3. Vercel автоматически:
   - Соберет проект
   - Задеплоит
   - Выдаст preview URL для PR

## 📱 Desktop версия

После деплоя на Vercel можно создать desktop версию:

```bash
cd electron
# Измените startUrl в main.js на ваш Vercel URL
npm run build
```

## 🆘 Troubleshooting

### Проблема: Белый экран после деплоя

**Решение:**
- Проверьте переменные окружения в Vercel
- Проверьте консоль браузера на ошибки
- Убедитесь, что `VITE_API_URL` правильный

### Проблема: API запросы не работают

**Решение:**
- Проверьте CORS на backend
- Проверьте `ALLOWED_ORIGINS` на backend
- Убедитесь, что backend доступен

### Проблема: WebSocket не работает

**Решение:**
- WebSocket должен быть на отдельном сервере
- Используйте `wss://` для production
- Проверьте настройки proxy на backend

---

**Статус**: ✅ Готов к деплою  
**Время деплоя**: ~5 минут  
**Стоимость**: Бесплатно (Hobby план)
