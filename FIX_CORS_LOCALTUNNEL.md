# Исправление CORS для LocalTunnel

## ✅ Что исправлено

1. **CORS middleware обновлен** в `server-go/main.go`:
   - Добавлена поддержка `.loca.lt` доменов (LocalTunnel)
   - Добавлена поддержка `.ngrok.io` и `.trycloudflare.com`
   - Улучшена проверка Vercel доменов (включая `vercel.live`)

## 🔧 Что нужно сделать

### 1. Перезапустить бэкенд

```bash
START_LOCAL_SERVER.bat
```

Или вручную:
```bash
cd server-go
go build -o main.exe .
main.exe
```

### 2. Убедиться, что туннель запущен

```bash
npx localtunnel --port 8080
```

Скопируйте полученный URL (например: `https://late-towns-wash.loca.lt`)

### 3. Обновить VITE_API_URL в Vercel

1. Vercel Dashboard → Your Project → Settings → Environment Variables
2. Найдите `VITE_API_URL`
3. Установите значение на ваш туннель URL:
   ```
   https://late-towns-wash.loca.lt
   ```
4. Сохраните и **Redeploy** проект

### 4. Альтернатива: использовать ALLOW_ALL_ORIGINS

Если проблемы продолжаются, создайте `server-go/.env`:

```env
ALLOW_ALL_ORIGINS=true
```

Это разрешит все origins (только для разработки!).

## 🧪 Проверка

1. Откройте сайт на Vercel
2. Откройте DevTools (F12) → Console
3. Попробуйте войти
4. Не должно быть CORS ошибок

## ⚠️ Важно

- **LocalTunnel URL меняется** при каждом перезапуске
- После перезапуска туннеля обновите `VITE_API_URL` в Vercel
- Для production используйте постоянный домен
