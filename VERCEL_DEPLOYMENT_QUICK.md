# ⚡ Быстрый деплой SafeGram на Vercel

## 🚀 За 5 минут

### 1. Подготовка (1 минута)

```bash
cd web
```

Убедитесь, что в `package.json` есть:
```json
{
  "scripts": {
    "build": "vite build"
  }
}
```

### 2. Установка Vercel CLI (1 минута)

```bash
npm i -g vercel
```

### 3. Логин (1 минута)

```bash
vercel login
```

### 4. Деплой (2 минуты)

```bash
# Preview деплой
vercel

# Production деплой
vercel --prod
```

### 5. Настройка переменных окружения

В Vercel Dashboard → Settings → Environment Variables добавьте:

```
VITE_API_URL=https://your-api-domain.com
VITE_WS_URL=wss://your-api-domain.com
```

## ✅ Готово!

Ваш сайт будет доступен по адресу:
- Preview: `https://your-project-xxx.vercel.app`
- Production: `https://your-project.vercel.app` (если настроен домен)

## 📝 Важные замечания

1. **Backend должен быть отдельно** - Vercel только для frontend
2. **CORS** - убедитесь, что backend разрешает запросы с вашего Vercel домена
3. **WebSocket** - нужен отдельный сервер для WebSocket (не работает на Vercel Serverless)
4. **Переменные окружения** - все `VITE_*` переменные должны быть в Vercel Dashboard

## 🔧 Настройка кастомного домена

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS записи (A, CNAME)
4. Vercel автоматически выдаст SSL сертификат

---

**Время деплоя**: ~5 минут  
**Стоимость**: Бесплатно (Hobby план)
