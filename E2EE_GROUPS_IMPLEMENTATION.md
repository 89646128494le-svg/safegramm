# 🔒 Полная E2EE для групп - Реализация

## ✅ Реализовано

### Backend (Go)

1. **Модель GroupKey** (`server-go/internal/models/group_key.go`)
   - Хранение зашифрованных групповых ключей для каждого участника
   - Версионирование ключей (keyVersion)
   - Отслеживание создателя ключа

2. **API Endpoints** (`server-go/internal/api/group_e2ee.go`)
   - `GET /api/chats/:id/group-key` - Получить зашифрованный ключ для текущего пользователя
   - `POST /api/chats/:id/group-key/init` - Инициализировать групповой ключ (owner/admin)
   - `POST /api/chats/:id/group-key/update` - Обновить ключ (forward secrecy)
   - `GET /api/chats/:id/group-key/version` - Получить версию ключа

3. **Модель Message** (`server-go/internal/models/message.go`)
   - Добавлено поле `Ciphertext` для хранения зашифрованных сообщений
   - Поддержка `ExpiresMs` для самоуничтожающихся сообщений

4. **API CreateMessage** (`server-go/internal/api/messages.go`)
   - Принимает `ciphertext` вместо `text` для E2EE сообщений
   - Поддержка `expiresMs` для таймеров

### Frontend (React/TypeScript)

1. **Инициализация E2EE** (`web/src/components/EnhancedChatWindow.tsx`)
   - `initializeGroupE2EE()` - Автоматическая инициализация при открытии группы
   - Генерация группового ключа (AES-GCM 256-bit)
   - Обёртывание ключа для каждого участника через ECDH

2. **Загрузка ключа** (`loadGroupKey()`)
   - Автоматическая загрузка при открытии группы
   - Развёртывание ключа из зашифрованного конверта

3. **Обновление ключа** (`updateGroupKey()`)
   - Forward secrecy - новый ключ при изменении состава
   - Автоматическое обновление при добавлении/удалении участников

4. **Шифрование сообщений**
   - Автоматическое шифрование текста перед отправкой
   - Использование AES-GCM с групповым ключом

5. **Расшифровка сообщений**
   - Автоматическая расшифровка при загрузке
   - Обработка ошибок расшифровки

## 🔐 Как это работает

### Инициализация

1. Owner/Admin открывает группу
2. Система проверяет наличие ключа
3. Если ключа нет:
   - Генерируется групповой ключ (AES-GCM 256-bit)
   - Для каждого участника:
     - Получается публичный ключ пользователя
     - Групповой ключ оборачивается через ECDH
   - Зашифрованные ключи сохраняются на сервере

### Отправка сообщения

1. Пользователь пишет сообщение
2. Если E2EE включен:
   - Текст шифруется групповым ключом (AES-GCM)
   - Отправляется `ciphertext` вместо `text`
3. Сервер сохраняет только шифротекст

### Получение сообщения

1. Сообщение приходит с `ciphertext`
2. Используется групповой ключ для расшифровки
3. Расшифрованный текст отображается пользователю

### Forward Secrecy (Обновление ключа)

1. При добавлении/удалении участника:
   - Генерируется новый групповой ключ
   - Старые ключи удаляются (soft delete)
   - Новый ключ оборачивается для всех текущих участников
   - Версия ключа увеличивается

2. Старые сообщения остаются зашифрованными старым ключом
3. Новые сообщения шифруются новым ключом

## 🛡️ Безопасность

- **ECDH P-256** для обмена ключами
- **AES-GCM 256-bit** для шифрования сообщений
- **Forward Secrecy** - ключи обновляются при изменении состава
- **Версионирование** - отслеживание версий ключей
- **Сервер не видит** открытый текст сообщений

## 📝 Использование

### Автоматическое

E2EE автоматически включается для групп и каналов:
- При первом открытии группы owner/admin инициализирует ключ
- Все участники автоматически получают ключ
- Сообщения автоматически шифруются/расшифровываются

### Ручное обновление ключа

В настройках группы (owner/admin):
- Можно вызвать `updateGroupKey()` для обновления ключа
- Полезно при подозрении на компрометацию

## ⚠️ Ограничения

1. **Только текст** - медиафайлы пока не шифруются (можно добавить)
2. **Нет проверки отпечатков** - нужно добавить UI для проверки ключей
3. **Нет истории ключей** - старые сообщения могут стать нечитаемыми после обновления ключа

## 🚀 Будущие улучшения

- [ ] Шифрование медиафайлов
- [ ] Проверка отпечатков ключей
- [ ] История ключей для чтения старых сообщений
- [ ] Автоматическое обновление ключей по расписанию
- [ ] Поддержка E2EE для каналов с подписчиками
