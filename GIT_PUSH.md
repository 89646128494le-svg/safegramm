# 📤 Инструкция по загрузке в GitHub

## ⚠️ ВАЖНО: Git был инициализирован в неправильной директории!

Git был инициализирован в домашней директории пользователя. Нужно работать в директории проекта.

## ✅ Правильные команды:

Откройте PowerShell или CMD в директории проекта:
```
c:\Users\Lev\Desktop\Проекты\SafeGram перезапуск
```

Выполните команды:

```powershell
# 1. Перейдите в директорию проекта
cd "c:\Users\Lev\Desktop\Проекты\SafeGram перезапуск"

# 2. Удалите неправильный git репозиторий (если он есть в домашней директории)
# НЕ ВЫПОЛНЯЙТЕ эту команду, если не уверены!

# 3. Инициализируйте git в правильной директории
git init

# 4. Добавьте remote
git remote add origin https://github.com/89646128494le-svg/SafeGram3.git
# Или, если remote уже существует:
git remote set-url origin https://github.com/89646128494le-svg/SafeGram3.git

# 5. Добавьте только файлы проекта (игнорируя системные файлы)
git add .

# 6. Создайте коммит
git commit -m "Initial commit: SafeGram multi-page website with admin panel"

# 7. Переименуйте ветку в main
git branch -M main

# 8. Запушьте в GitHub
git push -u origin main
```

## 🎯 Или используйте готовый скрипт:

Создайте файл `push-to-github.bat` в корне проекта:

```batch
@echo off
cd /d "%~dp0"
git init
git remote add origin https://github.com/89646128494le-svg/SafeGram3.git 2>nul
git remote set-url origin https://github.com/89646128494le-svg/SafeGram3.git
git add .
git commit -m "Initial commit: SafeGram multi-page website with admin panel and service management"
git branch -M main
git push -u origin main
pause
```

Запустите этот файл двойным кликом!

## 📝 Полная инструкция по деплою:

Откройте файл `DEPLOY_AND_API_KEYS.md` для полной инструкции по:
- Деплою на Vercel
- Получению API ключей
- Настройке переменных окружения
