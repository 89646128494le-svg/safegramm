# 🔗 Настройка Webhook на Windows

Вы находитесь в **Docker Desktop**, а не в WSL. Вот правильные способы запуска webhook receiver на Windows.

## ⚠️ Важно: Разница между Docker Desktop и WSL

- **Docker Desktop** - виртуальная машина для Docker, не имеет доступа к файлам Windows по `/mnt/c/`
- **WSL** - подсистема Linux для Windows, имеет доступ к файлам через `/mnt/c/`
- **Windows CMD/PowerShell** - нативный терминал Windows

## 🚀 Вариант 1: Запуск в Windows (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Установите Node.js (если еще не установлен)

Скачайте и установите с https://nodejs.org/

### Шаг 2: Запустите Webhook Receiver

**В PowerShell или CMD:**

```powershell
# Перейдите в директорию проекта
cd "C:\Users\Lev\Desktop\Проекты\SafeGram перезапуск\webhook-receiver"

# Установите зависимости (первый раз)
npm install

# Запустите
npm start
```

**Или используйте готовый скрипт:**

```powershell
.\start-windows.bat
```

### Шаг 3: Настройте Ngrok (для VPS)

**В новом терминале PowerShell:**

```powershell
# Установите ngrok для Windows
# Скачайте с https://ngrok.com/download
# Распакуйте в папку (например C:\ngrok)

# Или используйте через npm (если установлен)
npm install -g ngrok

# Запустите туннель
ngrok http 3000
```

Вы получите URL вида: `https://xxxx.ngrok-free.app`

### Шаг 4: Настройте на VPS

На VPS добавьте в `.env`:
```
WEBHOOK_URL=https://xxxx.ngrok-free.app/webhook
```

---

## 🚀 Вариант 2: Запуск в WSL

### Шаг 1: Откройте WSL терминал

В PowerShell:
```powershell
wsl
```

Или найдите "Ubuntu" в меню Пуск.

### Шаг 2: Перейдите в директорию проекта

```bash
# В WSL пути работают через /mnt/c/
cd /mnt/c/Users/Lev/Desktop/Проекты/SafeGram\ перезапуск/webhook-receiver
```

**Если не работает, попробуйте:**
```bash
# Проверьте, что папка существует
ls /mnt/c/Users/Lev/Desktop/Проекты/

# Если не видно, проверьте регистр букв
ls /mnt/c/Users/lev/Desktop/Проекты/  # маленькая 'l'
```

### Шаг 3: Запустите Receiver

```bash
# Установите зависимости
npm install

# Запустите
npm start
```

### Шаг 4: Настройте Ngrok в WSL

**В новом WSL терминале:**

```bash
# Установите ngrok
wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
tar xvzf ngrok-v3-stable-linux-amd64.tgz
sudo mv ngrok /usr/local/bin/

# Запустите туннель
ngrok http 3000
```

---

## 🚀 Вариант 3: Копирование файлов в Docker Desktop

Если хотите использовать Docker Desktop:

### Шаг 1: Скопируйте файлы

**В PowerShell:**
```powershell
# Создайте папку в Docker Desktop
docker run --rm -v webhook-data:/data alpine mkdir -p /data/receiver

# Скопируйте файлы
docker cp "C:\Users\Lev\Desktop\Проекты\SafeGram перезапуск\webhook-receiver" webhook-data:/data/receiver
```

Но проще использовать Windows или WSL напрямую.

---

## 🔍 Проверка правильного пути

### В Docker Desktop проверьте доступность диска C:

```bash
# Проверьте, доступен ли диск C
ls /mnt/

# Если видите 'c', попробуйте:
ls /mnt/c/Users/

# Если не видно, Docker Desktop может не монтировать C: автоматически
```

**Решение:** Используйте Windows напрямую или WSL.

---

## 📋 Быстрая команда для Windows

Создайте файл `start.bat` в корне проекта:

```batch
@echo off
cd "%~dp0webhook-receiver"
if not exist node_modules npm install
npm start
```

Затем просто запустите `start.bat` двойным кликом.

---

## ✅ Рекомендация

**Для Windows лучше всего:**

1. **Запустите webhook receiver в Windows** (PowerShell/CMD):
   ```powershell
   cd "C:\Users\Lev\Desktop\Проекты\SafeGram перезапуск\webhook-receiver"
   npm install
   npm start
   ```

2. **Используйте ngrok для Windows** или установите через npm:
   ```powershell
   npm install -g ngrok
   ngrok http 3000
   ```

3. **На VPS укажите ngrok URL** в `.env`

Это самый простой способ для Windows!

---

## 🐛 Troubleshooting

### Проблема: npm не найден

**Решение:**
- Установите Node.js с https://nodejs.org/
- Перезапустите терминал после установки

### Проблема: Порты заняты

**Решение:**
```powershell
# Проверьте, что использует порт 3000
netstat -ano | findstr :3000

# Остановите процесс или используйте другой порт
# В index.js измените PORT на 3001
```

### Проблема: Docker Desktop не видит файлы Windows

**Решение:**
- Используйте Windows напрямую (PowerShell/CMD)
- Или используйте WSL вместо Docker Desktop

---

## 📚 Дополнительно

- Полная инструкция: `WEBHOOK_VPS_SETUP.md`
- Быстрый старт: `WEBHOOK_VPS_QUICK.md`
