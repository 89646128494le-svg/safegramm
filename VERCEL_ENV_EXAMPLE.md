# 🔧 Переменные окружения для Vercel

Список всех переменных окружения, которые нужно добавить в Vercel.

---

## 📋 Переменные для Frontend (Vercel)

Добавьте эти переменные в **Vercel Dashboard** → Ваш проект → **Settings** → **Environment Variables**

### Обязательные переменные:

```env
# API URL - URL вашего backend (туннель или сервер)
VITE_API_URL=https://my-safegram.loca.lt

# GIPHY API Key (для GIF поиска)
VITE_GIPHY_API_KEY=ваш-giphy-api-key
```

### Опциональные переменные:

```env
# WebSocket URL (обычно тот же что и API)
VITE_WS_URL=wss://my-safegram.loca.lt

# Environment
VITE_NODE_ENV=production
```

---

## 🔑 Где получить API ключи

### GIPHY API Key:

1. Зайдите на https://developers.giphy.com/
2. Зарегистрируйтесь (бесплатно)
3. Создайте новое приложение
4. Скопируйте API Key
5. Добавьте в Vercel как `VITE_GIPHY_API_KEY`

**Или** используйте публичный ключ для тестирования:
```
VITE_GIPHY_API_KEY=GlVGYHkr3WSBnllca54iNt0yFbjz7L65
```
(Но лучше создать свой для production)

---

## 📝 Шаги добавления в Vercel

### 1. Зайдите в Vercel Dashboard

1. https://vercel.com/dashboard
2. Выберите ваш проект SafeGram

### 2. Откройте Settings

1. Вкладка **Settings**
2. Левое меню → **Environment Variables**

### 3. Добавьте переменные

Для каждой переменной:

1. Нажмите **Add New**
2. Введите **Key** (например: `VITE_API_URL`)
3. Введите **Value** (например: `https://my-safegram.loca.lt`)
4. Выберите окружения:
   - ✅ **Production**
   - ✅ **Preview**
   - ✅ **Development**
5. Нажмите **Save**

### 4. Повторите для всех переменных

Добавьте все переменные из списка выше.

### 5. Перезапустите деплой

После добавления всех переменных:

1. Перейдите на вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки)
4. Выберите **Redeploy**

---

## 📋 Минимальный набор (для начала работы)

Для быстрого старта добавьте минимум:

```env
VITE_API_URL=https://my-safegram.loca.lt
```

Остальное можно добавить позже.

---

## 🔄 Обновление VITE_API_URL

**Важно:** Когда вы перезапускаете туннель (LocalTunnel/ngrok), URL меняется!

Каждый раз когда URL туннеля меняется:

1. Обновите `VITE_API_URL` в Vercel
2. Redeploy проект

**Или** используйте Cloudflare Tunnel для постоянного URL.

---

## ✅ Проверка

После добавления переменных:

1. Redeploy проект на Vercel
2. Откройте сайт
3. F12 → Console
4. Проверьте что нет ошибок загрузки переменных

---

## 🆘 Troubleshooting

### Проблема: Переменные не загружаются

**Решение:**
- Убедитесь что переменные начинаются с `VITE_`
- Убедитесь что выбраны все окружения (Production, Preview, Development)
- Перезапустите деплой после добавления переменных

### Проблема: API URL не работает

**Решение:**
- Проверьте что туннель запущен
- Проверьте что URL правильный (начинается с `https://`)
- Проверьте что backend работает: `https://your-tunnel-url/health`

---

## 📚 Связанные инструкции

- Создание owner аккаунта: `CREATE_OWNER.bat`
- Настройка backend: `server-go/.env.example`
- Проверка подключения: `CHECK_CONNECTION_AND_LOGS.md`
