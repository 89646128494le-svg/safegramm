# 🔧 Перемещение конфигурации Vercel в web/

## Проблема
`rootDirectory` в корневом `vercel.json` может не работать правильно с `installCommand`. Vercel может не переходить в папку `web/` перед установкой зависимостей.

## ✅ Решение

Я создал `web/vercel.json` - конфигурацию прямо в папке `web/`. Теперь нужно:

### Шаг 1: Удалить корневой `vercel.json` (опционально)

Корневой `vercel.json` больше не нужен, так как конфигурация теперь в `web/vercel.json`. Но можно оставить оба - Vercel будет использовать тот, что в `web/`.

### Шаг 2: Настроить Root Directory в Vercel Dashboard

**В Vercel Dashboard:**
1. Откройте проект
2. **Settings** → **General**
3. Найдите **Root Directory**
4. Установите: `web`
5. Сохраните изменения

### Шаг 3: Закоммитьте и запушьте изменения

```bash
git add web/vercel.json
git commit -m "Move vercel.json to web/ directory for proper build configuration"
git push
```

### Шаг 4: Очистите кеш и пересоберите

**В Vercel Dashboard:**
1. **Deployments** → выберите последний деплой
2. **Redeploy** → отключите **Use existing Build Cache**
3. Или **Settings** → **General** → **Clear Build Cache**

## Почему это должно работать

- `web/vercel.json` будет использоваться автоматически, когда Root Directory = `web`
- Все команды (`installCommand`, `buildCommand`) выполняются в контексте `web/`
- Не нужно использовать `cd web` в командах
- Более чистая и понятная конфигурация

## Альтернатива: Оставить оба файла

Если хотите оставить корневой `vercel.json` для других целей, это тоже работает. Vercel будет использовать `web/vercel.json` когда Root Directory = `web`.

---

**Статус**: ✅ Создан `web/vercel.json`. Нужно настроить Root Directory в Dashboard и закоммитить изменения.
