# 🔧 КРИТИЧНО: Настройка Root Directory в Vercel

## ❌ Проблема

```
sh: line 1: vite: command not found
Error: Command "npm run build" exited with 127
```

Vercel не находит Vite, потому что **Root Directory не установлен правильно**.

---

## ✅ РЕШЕНИЕ (ОБЯЗАТЕЛЬНО!)

### Шаг 1: Установите Root Directory в Vercel

1. Зайдите в **Vercel Dashboard**
2. Ваш проект → **Settings**
3. **General** → прокрутите до **Root Directory**
4. Введите: `web`
5. Нажмите **Save**

### Шаг 2: Проверьте Framework Settings

1. **Settings** → **General** → **Framework Settings**
2. Убедитесь что:
   - **Build Command**: `npm run build` (или Override = Off)
   - **Output Directory**: `dist` (или Override = Off)
   - **Install Command**: `npm install` (или Override = Off)

### Шаг 3: Перезапустите деплой

1. **Deployments** → последний деплой
2. **"..."** → **Redeploy**

---

## ✅ Что исправлено в коде

1. ✅ Vite перемещен в `dependencies` (нужен для сборки)
2. ✅ TypeScript перемещен в `dependencies` (нужен для сборки)
3. ✅ `web/vercel.json` настроен правильно
4. ✅ Корневой `vercel.json` удален (конфликт)

---

## 📋 Проверка

После установки Root Directory = `web`:

1. ✅ Vercel будет использовать `web/package.json`
2. ✅ Vercel будет использовать `web/vercel.json`
3. ✅ Команды будут выполняться из папки `web/`
4. ✅ Vite будет найден и установлен

---

## 🆘 Если все равно не работает

### Проверьте:

1. **Root Directory** точно установлен в `web`?
2. Сохранили изменения в Settings?
3. Перезапустили деплой после изменений?

### Альтернатива:

Если Root Directory не работает, можно создать корневой `vercel.json`:

```json
{
  "version": 2,
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/dist",
  "installCommand": "cd web && npm install",
  "framework": "vite"
}
```

Но **лучше использовать Root Directory = web** - это правильный способ!

---

## ✅ Итог

**Главное:** Установите **Root Directory = `web`** в настройках Vercel!

Без этого Vercel не найдет `web/package.json` и `web/vercel.json`, и команды будут выполняться из корня проекта.
